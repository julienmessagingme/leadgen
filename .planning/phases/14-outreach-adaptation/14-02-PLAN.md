---
phase: 14-outreach-adaptation
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/tasks/task-b-invitations.js
  - src/tasks/task-c-followup.js
  - src/tasks/task-d-email.js
  - src/tasks/task-e-whatsapp.js
autonomous: true
requirements: [OUTR-03]

must_haves:
  truths:
    - "Les leads cold avec status new/enriched/scored sont selectionnes par Task B et recoivent une invitation"
    - "Les leads cold avec status invitation_sent progressent normalement dans Task C (connexion detection + follow-up)"
    - "Les leads cold avec status invitation_sent depuis 7+ jours recoivent un email via Task D (si email disponible, sinon skip)"
    - "Les leads cold avec status email_sent ou invitation_sent depuis 14+ jours recoivent un WhatsApp via Task E (si phone disponible, sinon skip)"
    - "Les leads cold sans email passent directement de invitation a follow-up, skip email, WhatsApp si phone dispo"
    - "Aucun message de la sequence cold ne fait reference a un signal LinkedIn"
  artifacts:
    - path: "src/tasks/task-b-invitations.js"
      provides: "Cold leads included in invitation batch, cold-aware note generation"
    - path: "src/tasks/task-c-followup.js"
      provides: "Cold leads get follow-up messages without signal references"
    - path: "src/tasks/task-d-email.js"
      provides: "Cold leads get email J+7, leads without email gracefully skipped"
    - path: "src/tasks/task-e-whatsapp.js"
      provides: "Cold leads get WhatsApp J+14 if phone available"
  key_links:
    - from: "src/tasks/task-b-invitations.js"
      to: "src/lib/message-generator.js"
      via: "generateInvitationNote(lead, templates) — cold branch auto-detected by isColdLead"
      pattern: "generateInvitationNote"
    - from: "src/tasks/task-d-email.js"
      to: "src/lib/message-generator.js"
      via: "generateEmail(lead, templates) — cold branch auto-detected"
      pattern: "generateEmail"
---

<objective>
Ensure cold leads flow through the existing outreach sequence (Tasks B/C/D/E) without any signal references in messages.

Purpose: Cold leads injected by Phase 13 pipeline have status "new" and signal_category "cold_outbound". They must progress through the same sequence as signal-based leads (invitation -> follow-up -> email J+7 -> WhatsApp J+14) but with cold-adapted messages. Leads without email skip the email step; leads without phone skip WhatsApp.

Output: Modified task-b, task-c, task-d, task-e files that handle cold leads alongside signal leads.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-outreach-adaptation/14-CONTEXT.md
@.planning/phases/14-outreach-adaptation/14-01-SUMMARY.md
@src/tasks/task-b-invitations.js
@src/tasks/task-c-followup.js
@src/tasks/task-d-email.js
@src/tasks/task-e-whatsapp.js
@src/lib/message-generator.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Task B invitation — include cold leads and verify cold message content</name>
  <files>src/tasks/task-b-invitations.js</files>
  <action>
Task B already selects leads with status `new`/`enriched`/`scored` and tier `hot`/`warm`. Cold leads from Phase 13 pipeline are inserted with status `new`. However, cold leads may have tier `cold` (from ICP scoring) which means they would be excluded.

**Modifications:**

1. **Tier filter for cold leads:** Change the lead selection query to also include leads with `signal_category = 'cold_outbound'` regardless of tier. Do this by running two queries: the existing one (hot/warm, any signal), and a second one specifically for cold leads (status new/enriched/scored, signal_type = 'cold_search') without tier filter. Merge and dedup by ID, then slice to `remaining` limit. Sort by icp_score descending.

   Alternative simpler approach: Add `"cold"` to the tier filter `.in("tier", ["hot", "warm", "cold"])` so all cold-scored leads are also picked up. This is acceptable since cold leads are pre-qualified through Sales Nav filtering. Use this simpler approach.

2. **Invitation character limit for cold:** After generating the note, if the lead is cold (check `lead.signal_type === "cold_search"` or `lead.signal_category === "cold_outbound"` or `lead.metadata?.cold_outbound === true`), enforce 200 char limit instead of 280. Add this check right after the note is generated:
   ```
   if (isCold && note.length > 200) {
     note = note.substring(0, 197) + "...";
   }
   ```

3. **Log distinction:** In the log line after sending, add "(cold)" suffix if the lead is cold: `"Invitation sent to " + name + " (" + note.length + " chars" + (isCold ? ", cold" : "") + ")"`.

No changes to BeReach invitation sending — it's the same connectProfile API call regardless of lead type. The message-generator.js (modified in plan 14-01) handles the cold-specific prompt automatically via isColdLead().
  </action>
  <verify>
SSH to VPS: `cd /home/openclaw/leadgen && node -e "const t = require('./src/tasks/task-b-invitations.js'); console.log('task-b loaded, type:', typeof t)"` — must print function type without errors.
  </verify>
  <done>
Task B selects cold leads (tier "cold" included), generates cold-adapted invitation notes via message-generator cold branch, enforces 200 char limit for cold leads, and logs cold status.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tasks C/D/E — cold lead sequence progression</name>
  <files>src/tasks/task-c-followup.js, src/tasks/task-d-email.js, src/tasks/task-e-whatsapp.js</files>
  <action>
The message generation adaptation is already handled by message-generator.js (plan 14-01). The tasks mostly work as-is because they query by status (invitation_sent, connected, email_sent) which cold leads will naturally reach. However, there are specific adjustments needed:

**Task C (task-c-followup.js):**
- No query changes needed. Cold leads with status `invitation_sent` will be detected as connected when their invitation is accepted, same as signal leads.
- The `generateFollowUpMessage(lead, templates)` call already handles cold detection via isColdLead in message-generator.
- Add log annotation: after the follow-up sent log, add "(cold)" if lead is cold. Check via `lead.metadata?.cold_outbound === true || lead.signal_type === "cold_search"`.

**Task D (task-d-email.js):**
- **Tier filter:** The `selectLeads` function filters `.in("tier", ["hot", "warm"])`. Add `"cold"` to include cold leads: `.in("tier", ["hot", "warm", "cold"])`.
- **Email skip for no_email cold leads:** Cold leads may have `email_status: "no_email"` in their metadata. The existing flow already handles this gracefully — `checkEmail` tries FullEnrich, returns null if no email, and the lead is skipped with `skipped.no_email++`. This is the correct behavior per CONTEXT (skip email J+7 for leads sans email). No change needed here.
- The `generateEmail(lead, templates)` call handles cold detection automatically.
- Add log annotation for cold leads in the "Email sent successfully" log line.

**Task E (task-e-whatsapp.js):**
- **Tier filter:** Both queries filter `.in("tier", ["hot", "warm"])`. Add `"cold"` to both: `.in("tier", ["hot", "warm", "cold"])`.
- **Phone availability:** The existing `.not("phone", "is", null)` filter already ensures only leads with phone numbers are selected. Cold leads without phone are naturally excluded. This matches CONTEXT (WhatsApp J+14 si numero disponible).
- The `generateWhatsAppBody(lead, templates)` call handles cold detection automatically.
- Add log annotation for cold leads in the "Template created" log line.

**Summary of changes per file:**
- task-c-followup.js: Add cold lead log annotation only (1 line change)
- task-d-email.js: Add "cold" to tier filter in selectLeads, add cold log annotation (2 changes)
- task-e-whatsapp.js: Add "cold" to both tier filters (2 query changes), add cold log annotation (1 change)
  </action>
  <verify>
SSH to VPS and verify all three task files load without errors:
```
cd /home/openclaw/leadgen
node -e "const c = require('./src/tasks/task-c-followup.js'); const d = require('./src/tasks/task-d-email.js'); const e = require('./src/tasks/task-e-whatsapp.js'); console.log('All tasks loaded:', typeof c, typeof d, typeof e)"
```
Must print "All tasks loaded: function function function".
  </verify>
  <done>
Cold leads progress through the full outreach sequence: invitation (Task B) -> connection detection + follow-up (Task C) -> email J+7 if email available (Task D) -> WhatsApp J+14 if phone available (Task E). All messages are cold-adapted (no signal references) via message-generator.js cold branch. Leads without email skip email step, leads without phone skip WhatsApp step.
  </done>
</task>

</tasks>

<verification>
1. Task B: cold leads with tier "cold" are selected alongside hot/warm leads
2. Task B: cold invitation notes are limited to 200 chars
3. Task C: cold leads in "connected" status get signal-free follow-up messages
4. Task D: cold leads with "invitation_sent" since 7+ days get emails (if email available)
5. Task D: cold leads without email are gracefully skipped
6. Task E: cold leads get WhatsApp J+14 (if phone available)
7. All task files load without syntax errors
8. Cold lead status progression: new -> invitation_sent -> connected -> email_sent/whatsapp
</verification>

<success_criteria>
- All 4 task files include cold leads in their selection queries (tier "cold" added)
- Cold leads progress through the same sequence timing as signal leads
- No message in the cold sequence references a signal
- Leads without email skip email step, leads without phone skip WhatsApp
- All task files load without errors on VPS
</success_criteria>

<output>
After completion, create `.planning/phases/14-outreach-adaptation/14-02-SUMMARY.md`
</output>
