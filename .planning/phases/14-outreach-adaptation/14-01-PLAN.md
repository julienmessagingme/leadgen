---
phase: 14-outreach-adaptation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/message-generator.js
  - src/api/settings.js
  - frontend/src/components/settings/ColdTemplatesTab.jsx
  - frontend/src/pages/Settings.jsx
autonomous: true
requirements: [OUTR-01, OUTR-02]

must_haves:
  truths:
    - "Un lead cold recoit un message d'invitation genere par Claude qui ne mentionne JAMAIS un signal LinkedIn"
    - "Les 4 types de messages cold (invitation, followup, email, whatsapp) sont generes sans reference signal"
    - "Julien peut configurer plusieurs templates cold dans la page Settings"
    - "Chaque template cold contient un prompt et une proposition de valeur"
    - "Les messages signal-based continuent de fonctionner normalement"
  artifacts:
    - path: "src/lib/message-generator.js"
      provides: "Cold-aware message generation with separate cold templates and prompts"
      exports: ["generateInvitationNote", "generateFollowUpMessage", "generateEmail", "generateWhatsAppBody", "loadTemplates"]
    - path: "src/api/settings.js"
      provides: "CRUD for cold template settings"
      contains: "cold_templates"
    - path: "frontend/src/components/settings/ColdTemplatesTab.jsx"
      provides: "UI for managing multiple cold templates"
    - path: "frontend/src/pages/Settings.jsx"
      provides: "New tab for Templates Cold"
  key_links:
    - from: "src/lib/message-generator.js"
      to: "settings table"
      via: "loadTemplates fetches cold_templates key"
      pattern: "cold_templates"
    - from: "frontend/src/components/settings/ColdTemplatesTab.jsx"
      to: "/api/settings/config"
      via: "useConfig + useUpdateConfig hooks"
      pattern: "cold_templates"
---

<objective>
Adapt message generation for cold leads and add cold template management to Settings.

Purpose: Cold leads must receive personalized messages from Claude that never reference a LinkedIn signal. The existing signal-based message flow must remain untouched. Julien needs to manage multiple cold templates (each with a prompt/instructions and a value proposition) from the Settings dashboard.

Output: Modified message-generator.js with cold-aware branching, new cold template settings CRUD, new ColdTemplatesTab component in Settings.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-outreach-adaptation/14-CONTEXT.md
@src/lib/message-generator.js
@src/api/settings.js
@frontend/src/components/settings/TemplatesTab.jsx
@frontend/src/pages/Settings.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cold-aware message generation in message-generator.js</name>
  <files>src/lib/message-generator.js</files>
  <action>
Modify message-generator.js to detect cold leads and use cold-specific prompts instead of signal-based ones.

**Detection:** A lead is cold when `lead.signal_category === "cold_outbound"` OR `lead.metadata?.cold_outbound === true` OR `lead.signal_type === "cold_search"`.

Create helper `isColdLead(lead)` that checks these conditions.

**Cold default templates (4 new DEFAULT vars):**

- `DEFAULT_COLD_INVITATION_TEMPLATE`: "Redige une invitation LinkedIn courte et professionnelle pour ce prospect.\n\nRegles:\n- Presentation courte de Julien et MessagingMe\n- Proposition de valeur adaptee au secteur du prospect\n- Ton professionnel et direct\n- Max 200 caracteres STRICT (cible 150-200)\n- Pas d'emojis, pas de pitch commercial\n- NE JAMAIS mentionner un post, like, commentaire ou signal LinkedIn\n- Personnaliser avec le nom et l'entreprise (PAS le titre)"

- `DEFAULT_COLD_FOLLOWUP_TEMPLATE`: "Redige un message de suivi LinkedIn post-connexion pour un prospect contacte en cold.\n\nRegles:\n- Remercier pour la connexion\n- Proposer un echange sur les enjeux de messaging/communication client\n- Mentionner MessagingMe brievement\n- 3 a 5 phrases max\n- Ton naturel et direct\n- NE JAMAIS mentionner un signal LinkedIn"

- `DEFAULT_COLD_EMAIL_TEMPLATE`: "Redige un email de relance pour un prospect contacte en cold.\n\nRegles:\n- Objet accrocheur et court\n- Corps en HTML simple\n- Proposition de valeur MessagingMe pour leur secteur\n- CTA: lien Calendly {calendlyUrl}\n- Signature: Julien Poupard, DG MessagingMe\n- Ton professionnel mais personnel\n- NE JAMAIS mentionner un post, like ou signal LinkedIn"

- `DEFAULT_COLD_WHATSAPP_TEMPLATE`: "Redige un message WhatsApp pour un prospect contacte en cold.\n\nRegles:\n- 3 a 4 lignes max\n- Proposition de RDV via Calendly\n- Ton direct et personnel\n- Pas d'emojis excessifs\n- NE JAMAIS mentionner un signal LinkedIn"

**loadTemplates() modification:** Also load key `cold_templates` from settings table. This key stores a JSON array of templates: `[{name: string, prompt: string, value_proposition: string}]`. Return it alongside the existing template_ keys in the map.

**Each generate function modification (generateInvitationNote, generateFollowUpMessage, generateEmail, generateWhatsAppBody):**

Add cold branch at the top using `isColdLead(lead)`:
- If cold: use cold template instructions from `tpl.cold_templates` (pick first template, or if multiple exist use a random one for variety). If cold_templates not configured, use the `DEFAULT_COLD_*` constant.
- If cold: build user prompt WITHOUT signal_type/signal_detail lines. Use only: Prospect name, Entreprise, and the value proposition from the selected template.
- If cold: for invitation, enforce 200 char limit (not 280).
- If NOT cold: existing behavior unchanged.

The user prompt for cold leads should look like:
```
{cold_template_instructions}

Proposition de valeur a mettre en avant: {value_proposition}

Prospect: {full_name}
Entreprise: {company_name}

Reponds en JSON: {"note": "..."}
```

**Important:** Do NOT change the function signatures. The existing `(lead, templates)` params stay the same. The cold detection is purely based on lead data fields.
  </action>
  <verify>
Run `node -e "const m = require('./src/lib/message-generator.js'); console.log(typeof m.generateInvitationNote, typeof m.loadTemplates)"` on VPS via SSH to confirm module loads without syntax errors.
  </verify>
  <done>
message-generator.js detects cold leads via isColdLead(), uses cold-specific prompts without signal references, falls back to DEFAULT_COLD_* templates, and picks from configured cold_templates if available. Signal-based leads are completely unaffected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cold templates CRUD in settings API + Settings UI tab</name>
  <files>src/api/settings.js, frontend/src/components/settings/ColdTemplatesTab.jsx, frontend/src/pages/Settings.jsx</files>
  <action>
**Backend — src/api/settings.js:**

Add `"cold_templates"` to `ALLOWED_CONFIG_KEYS` array. This key stores a JSON string (array of template objects). The existing PATCH /config/:key endpoint with upsert already handles this — just adding the key to the allowlist is sufficient.

**Frontend — new ColdTemplatesTab.jsx:**

Create `frontend/src/components/settings/ColdTemplatesTab.jsx` that manages multiple cold templates stored as a JSON array under the `cold_templates` config key.

Structure:
- Use `useConfig()` and `useUpdateConfig()` hooks (same pattern as TemplatesTab.jsx).
- On load, parse `cold_templates` from config (JSON array or empty array default).
- Each template card shows: name (text input), prompt/instructions (textarea, min 4 rows), value proposition (textarea, min 2 rows).
- "Ajouter un template" button adds a new empty template card.
- Each card has a delete button (trash icon or "Supprimer" text).
- Single "Enregistrer" button at bottom saves the entire array as JSON string to `cold_templates` key via `updateConfig.mutateAsync({ key: "cold_templates", value: JSON.stringify(templates) })`.
- Show "Enregistre !" feedback for 2 seconds after save (same pattern as TemplatesTab).
- Tailwind styling consistent with existing settings tabs (bg-white rounded-lg shadow p-4 cards).
- Header: "Templates Cold Outbound" with description: "Configurez les templates utilises par l'IA pour generer les messages destines aux leads cold. Chaque template contient des instructions et une proposition de valeur."

**Frontend — Settings.jsx:**

Import ColdTemplatesTab. Add to TABS array: `{ key: "cold_templates", label: "Templates Cold" }` (place it after the existing "Templates" tab). Add to TAB_COMPONENTS: `cold_templates: ColdTemplatesTab`.
  </action>
  <verify>
1. SSH to VPS, run `cd /home/openclaw/leadgen && node -e "const s = require('./src/api/settings.js'); console.log('settings router loaded')"` to verify backend loads.
2. Run `cd /home/openclaw/leadgen/frontend && npx vite build` to verify frontend compiles without errors.
  </verify>
  <done>
cold_templates key is allowed in settings API. ColdTemplatesTab displays in Settings page with CRUD for multiple cold templates (name + prompt + value proposition). Frontend builds without errors.
  </done>
</task>

</tasks>

<verification>
1. Cold lead detection: a lead with `signal_category: "cold_outbound"` is identified as cold by `isColdLead()`
2. Message generation: cold leads get prompts without signal references, signal leads get existing prompts unchanged
3. Settings UI: Templates Cold tab appears in Settings, can add/edit/delete templates, save persists to Supabase
4. Template loading: message-generator loads cold_templates from settings and uses them for cold message generation
</verification>

<success_criteria>
- message-generator.js has isColdLead() and cold branches in all 4 generate functions
- Cold prompts never mention "signal", "post", "like", "commentaire"
- Settings page has "Templates Cold" tab with multi-template CRUD
- cold_templates key accepted by settings API
- Frontend builds without errors
- Existing signal-based message generation unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/14-outreach-adaptation/14-01-SUMMARY.md`
</output>
