---
phase: 10-query-optimization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scheduler.js
  - src/lib/anthropic.js
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "Logs older than 30 days are automatically deleted daily at 02:00"
    - "anthropic.js has no redundant dotenv.config() call"
  artifacts:
    - path: "src/scheduler.js"
      provides: "Log cleanup cron job registered"
      contains: "log-cleanup"
    - path: "src/lib/anthropic.js"
      provides: "Clean anthropic client without redundant dotenv"
  key_links:
    - from: "src/scheduler.js"
      to: "supabase logs table"
      via: "DELETE FROM logs WHERE created_at < 30 days ago"
      pattern: "\\.delete\\(\\)"
---

<objective>
Add a daily log cleanup cron job and remove a redundant dotenv.config() call from anthropic.js.

Purpose: The logs table grows unbounded. A daily cleanup of records older than 30 days prevents table bloat and keeps queries fast (leveraging the logs(task, created_at DESC) index from Phase 09). The redundant dotenv call is dead code cleanup.
Output: Updated scheduler.js with log-cleanup task, cleaned anthropic.js.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-query-optimization/10-RESEARCH.md
@src/scheduler.js
@src/lib/anthropic.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add log cleanup cron job to scheduler.js</name>
  <files>src/scheduler.js</files>
  <action>
Read src/scheduler.js to understand the existing registerTask pattern and supabase import.

Add a new cron job registered via the existing `registerTask` function (or equivalent pattern used in the file):
- Task name: "log-cleanup"
- Schedule: "0 2 * * *" (daily at 02:00, runs every day including weekends -- log cleanup has no LinkedIn rate limit concern)
- Handler: compute cutoff date as 30 days ago ISO string, then `await supabase.from("logs").delete().lt("created_at", cutoff)`. Log result with existing log() helper: "Deleted logs older than 30 days".
- If supabase.delete() supports `{ count: "exact" }`, use it and include count in log message. If not certain, just log "Log cleanup completed" without count.

Place the registration after the existing task registrations (task-a through task-f). Follow the exact same pattern as existing task registrations for consistency.
  </action>
  <verify>Run on VPS: `node -c src/scheduler.js` to verify syntax. Grep: `grep -n 'log-cleanup' src/scheduler.js` confirms registration.</verify>
  <done>Log cleanup cron job registered in scheduler.js, running daily at 02:00, deleting logs older than 30 days.</done>
</task>

<task type="auto">
  <name>Task 2: Remove redundant dotenv.config() from anthropic.js</name>
  <files>src/lib/anthropic.js</files>
  <action>
Read src/lib/anthropic.js. Find and remove the `require("dotenv").config()` or `dotenv.config()` call (expected around line 4 per research). The main entry point (server.js or index.js) already loads dotenv, making this call redundant.

Do NOT remove the Anthropic client initialization or any other code -- only remove the dotenv line.
  </action>
  <verify>Run on VPS: `node -c src/lib/anthropic.js` to verify syntax. Grep: `grep -n 'dotenv' src/lib/anthropic.js` returns nothing.</verify>
  <done>Redundant dotenv.config() removed from anthropic.js. File still exports working Anthropic client.</done>
</task>

</tasks>

<verification>
- scheduler.js contains "log-cleanup" cron registration at "0 2 * * *"
- Log cleanup deletes from logs table where created_at < 30 days ago
- anthropic.js has no dotenv import or config call
- Both files pass node -c syntax check
</verification>

<success_criteria>
Log cleanup runs automatically daily at 02:00 preventing unbounded log table growth. anthropic.js is clean of redundant dotenv call. No behavioral change to existing tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/10-query-optimization/10-03-SUMMARY.md`
</output>
