---
phase: 10-query-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/007-add-last-processed-run-id.sql
  - src/tasks/task-b-invitations.js
  - src/tasks/task-c-followup.js
  - src/tasks/task-d-email.js
  - src/tasks/task-e-whatsapp.js
  - src/tasks/task-f-briefing.js
  - src/tasks/whatsapp-poll.js
  - src/tasks/signal-collector.js
  - src/lib/icp-scorer.js
  - src/lib/message-generator.js
  - src/api/leads.js
autonomous: false
requirements: [PERF-04, PERF-05, PERF-06, PERF-07, PERF-08]

must_haves:
  truths:
    - "Bulk pause/resume updates all leads in a single query (not per-lead loop)"
    - "Task B and C skip already-processed leads via last_processed_run_id column (not ILIKE on logs)"
    - "Task queries select only needed columns (no select('*'))"
    - "All task queries have .limit() bounds"
    - "loadTemplates() is called once per task run, not once per lead"
  artifacts:
    - path: "src/db/migrations/007-add-last-processed-run-id.sql"
      provides: "ALTER TABLE leads ADD COLUMN last_processed_run_id"
      contains: "last_processed_run_id"
    - path: "src/api/leads.js"
      provides: "Batched bulk action updates"
      contains: ".in(\"id\""
    - path: "src/tasks/task-b-invitations.js"
      provides: "Optimized task B with column select, limit, run_id check, template caching"
    - path: "src/tasks/task-c-followup.js"
      provides: "Optimized task C with column select, limit, run_id check"
    - path: "src/lib/message-generator.js"
      provides: "Generator functions accepting optional templates parameter"
  key_links:
    - from: "src/tasks/task-b-invitations.js"
      to: "src/lib/message-generator.js"
      via: "generateInvitationNote(lead, templates)"
      pattern: "generateInvitationNote\\(.*,\\s*templates\\)"
    - from: "src/api/leads.js"
      to: "supabase leads table"
      via: ".update().in('id', ids) for batched bulk action"
      pattern: "\\.in\\(\"id\""
---

<objective>
Optimize all task query patterns: replace select("*") with specific columns, add .limit() bounds, replace ILIKE idempotence with lead flag, batch bulk actions, and cache template loading.

Purpose: Task queries currently fetch all columns, have no row limits, use expensive ILIKE for idempotence, update leads one-by-one in bulk actions, and reload templates from DB for every lead. These patterns waste bandwidth, risk unbounded memory, and cause unnecessary DB load.
Output: Optimized task files, batched bulk actions in leads.js, migration for last_processed_run_id column.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-query-optimization/10-RESEARCH.md
@src/tasks/task-b-invitations.js
@src/tasks/task-c-followup.js
@src/tasks/task-d-email.js
@src/tasks/task-e-whatsapp.js
@src/tasks/task-f-briefing.js
@src/tasks/whatsapp-poll.js
@src/tasks/signal-collector.js
@src/lib/icp-scorer.js
@src/lib/message-generator.js
@src/api/leads.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create run_id migration, batch bulk actions, and optimize task queries</name>
  <files>
    src/db/migrations/007-add-last-processed-run-id.sql
    src/api/leads.js
    src/tasks/task-b-invitations.js
    src/tasks/task-c-followup.js
    src/tasks/task-d-email.js
    src/tasks/task-e-whatsapp.js
    src/tasks/task-f-briefing.js
    src/tasks/whatsapp-poll.js
    src/tasks/signal-collector.js
    src/lib/icp-scorer.js
    src/lib/message-generator.js
  </files>
  <action>
**Migration file (007-add-last-processed-run-id.sql):**
- `ALTER TABLE leads ADD COLUMN IF NOT EXISTS last_processed_run_id text;`
- Follow existing convention in src/db/migrations/

**Batch bulk actions in leads.js (PERF-04):**
- Find the bulk-action endpoint (POST /bulk-action or similar)
- For pause: replace the per-lead update loop with a single `supabase.from("leads").update({ metadata: supabase.rpc or use raw SQL via .rpc() }).in("id", ids)`. Since metadata is JSONB and needs merge (not replace), use PostgreSQL `metadata || '{"is_paused": true, "paused_at": "..."}'::jsonb` pattern via an RPC function, OR fetch all leads first, compute merged metadata, then issue individual updates (acceptable since exclude is infrequent). Simplest approach: for pause/resume, if metadata merge is needed, keep the loop but batch the read. For exclude, the PII nullification logic (from 09-02) already requires per-lead processing, so keep that as-is.
- Actually simplest: just use `.update({ status: "paused" }).in("id", ids)` if pause/resume uses a status field. Read leads.js carefully to understand the actual implementation before changing.

**Replace ILIKE idempotence with lead flag (PERF-05):**
- In task-b-invitations.js: Find the ILIKE check on logs table. Replace with `if (lead.last_processed_run_id === runId) { skipped++; continue; }`. After successfully processing a lead, include `last_processed_run_id: runId` in the lead update payload.
- In task-c-followup.js: Same pattern -- replace any ILIKE idempotence check with last_processed_run_id check.
- Add `last_processed_run_id` to the select columns for task-b and task-c.

**Replace select("*") with specific columns (PERF-06):**
Use the exact column lists from the research inventory:
- task-b-invitations.js: `id, full_name, first_name, last_name, linkedin_url, headline, company_name, signal_type, signal_detail, metadata, email, icp_score, tier, status, last_processed_run_id`
- task-c-followup.js (line ~58): `id, full_name, first_name, last_name, linkedin_url, headline, company_name, signal_type, signal_detail, metadata, email, status, last_processed_run_id`
- task-c-followup.js (line ~105): `id, full_name, first_name, last_name, linkedin_url, headline, company_name, signal_type, signal_detail, metadata, email, follow_up_sent_at`
- task-d-email.js: `id, full_name, first_name, last_name, linkedin_url, headline, company_name, signal_type, signal_detail, metadata, email, icp_score, tier`
- task-e-whatsapp.js (both queries): `id, full_name, first_name, last_name, linkedin_url, phone, headline, company_name, signal_type, signal_detail, metadata, email, tier`
- task-f-briefing.js: `id, full_name, headline, company_name, company_sector, signal_type, signal_source, signal_detail, linkedin_url, icp_score`
- whatsapp-poll.js: `id, full_name, phone, email, linkedin_url, metadata, whatsapp_template_created_at, status`
- signal-collector.js (watchlist): `source_type, source_label, source_url, keywords, sequence_id`
- icp-scorer.js (icp_rules): `category, value, key, numeric_value, threshold`

IMPORTANT: Read each file first to find the exact line. The column lists above are from research -- verify they match what the code actually uses downstream. If a column is accessed later in the function but not in the research list, ADD it to the select.

**Add .limit() to unbounded queries (PERF-07):**
- task-c-followup.js (invitation_sent leads): `.limit(200)`
- task-c-followup.js (connected leads pending follow-up): `.limit(50)`
- task-d-email.js: `.limit(50)`
- task-e-whatsapp.js (both queries): `.limit(50)` each
- whatsapp-poll.js: `.limit(100)`

**Cache loadTemplates() per task run (PERF-08):**
- In message-generator.js: modify `generateInvitationNote`, `generateFollowUpMessage`, `generateEmail`, `generateWhatsAppBody` to accept an optional `templates` parameter. If provided, use it. If not, fall back to `await loadTemplates()` (backward compatible).
- In task-b-invitations.js: before the lead loop, add `var templates = await loadTemplates();` and pass to `generateInvitationNote(lead, templates)`.
- In task-c-followup.js: same pattern, `var templates = await loadTemplates();` before loop, pass to `generateFollowUpMessage(lead, templates)`.
- In task-d-email.js: same, pass to `generateEmail(lead, templates)`.
- In task-e-whatsapp.js: same, pass to `generateWhatsAppBody(lead, templates)`.
- Export loadTemplates from message-generator.js if not already exported.
  </action>
  <verify>
Run on VPS via SSH for each modified file: `node -c src/tasks/task-b-invitations.js` (and each other file) to verify syntax.
Grep to confirm no remaining `select("*")` in task files: `grep -rn 'select("*")' src/tasks/ src/lib/icp-scorer.js`
Grep to confirm .limit() present: `grep -rn '\.limit(' src/tasks/`
Grep to confirm no ILIKE idempotence: `grep -rn 'ilike' src/tasks/`
  </verify>
  <done>
All task queries use specific column selects and .limit() bounds. ILIKE idempotence replaced with last_processed_run_id flag in task-b and task-c. Bulk actions batched where possible. loadTemplates() cached per task run. Migration file ready for application.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Apply last_processed_run_id migration</name>
  <files>
    src/db/migrations/007-add-last-processed-run-id.sql
  </files>
  <action>
User must apply the ALTER TABLE migration to add last_processed_run_id column to the leads table. Claude cannot run SQL against Supabase directly. After applying, restart PM2 so the optimized task code takes effect.
  </action>
  <verify>
    1. Connect to psql on VPS or Supabase SQL Editor
    2. Run: ALTER TABLE leads ADD COLUMN IF NOT EXISTS last_processed_run_id text;
    3. Verify column exists: SELECT column_name FROM information_schema.columns WHERE table_name = 'leads' AND column_name = 'last_processed_run_id';
    4. Restart PM2: pm2 restart leadgen
    5. Check logs for any errors: pm2 logs leadgen --lines 20
  </verify>
  <done>
last_processed_run_id column exists on leads table. PM2 restarted with no errors. Type "applied" to confirm.
  </done>
</task>

</tasks>

<verification>
- No `select("*")` remaining in any task file or icp-scorer.js
- All task queries with lead lists have .limit() bounds
- task-b and task-c use last_processed_run_id instead of ILIKE on logs
- loadTemplates() called once before lead loops in task-b, task-c, task-d, task-e
- Bulk action in leads.js uses batched update pattern
- Migration file 007 exists
</verification>

<success_criteria>
All task queries are optimized: specific column selects, bounded with .limit(), ILIKE replaced with lead flag, templates cached per run, bulk actions batched. No behavioral change in task output -- same leads processed, same messages generated.
</success_criteria>

<output>
After completion, create `.planning/phases/10-query-optimization/10-02-SUMMARY.md`
</output>
