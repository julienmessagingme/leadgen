---
phase: 04-api-auth-react
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/scheduler.js
autonomous: false
requirements:
  - INFRA-02
user_setup:
  - service: nginx-proxy-manager
    why: "Domain proxy configuration in Nginx Proxy Manager UI"
    dashboard_config:
      - task: "Add Proxy Host: domain -> 127.0.0.1:3005, enable SSL with Let's Encrypt"
        location: "Nginx Proxy Manager UI (Docker on VPS)"

must_haves:
  truths:
    - "Port 3005 is NOT accessible from outside the VPS (bound to 127.0.0.1)"
    - "The dashboard is accessible via HTTPS through Nginx Proxy Manager"
    - "Supabase free tier does not pause due to weekend inactivity"
    - "PM2 process runs with new Express + scheduler without errors"
  artifacts:
    - path: "src/scheduler.js"
      provides: "Weekend keep-alive cron for Supabase"
      contains: "keep-alive"
  key_links:
    - from: "Nginx Proxy Manager"
      to: "127.0.0.1:3005"
      via: "proxy_pass HTTPS -> localhost"
      pattern: "proxy"
    - from: "src/scheduler.js"
      to: "supabase"
      via: "weekend cron ping"
      pattern: "cron.*schedule.*keep"
---

<objective>
Deploy the Express + React application securely behind Nginx Proxy Manager with HTTPS, verify port security, and add Supabase keep-alive.

Purpose: The dashboard must be accessible only through HTTPS (not directly on port 3005 from the internet). The Supabase free tier must not pause during weekends.
Output: Secure HTTPS access, port 3005 locked down, Supabase keep-alive active.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-api-auth-react/04-RESEARCH.md
@.planning/phases/04-api-auth-react/04-01-SUMMARY.md
@src/index.js
@src/scheduler.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase keep-alive cron and PM2 deployment</name>
  <files>
    src/scheduler.js
  </files>
  <action>
Add a Supabase keep-alive cron job to `src/scheduler.js` to prevent the free tier 7-day inactivity pause:

- Register a new cron task using the existing `registerTask` pattern (or direct `cron.schedule` if registerTask is not appropriate for a utility task).
- Schedule: `0 10 * * 0,6` (Saturday and Sunday at 10:00 Europe/Paris).
- Action: `supabase.from('leads').select('id', { count: 'exact', head: true })` -- lightweight count query.
- Log success: "Supabase keep-alive ping OK".
- Log failure: "Supabase keep-alive failed: {error}".
- Use the existing `{ timezone: 'Europe/Paris' }` pattern from other cron entries.

Then deploy everything on VPS:

1. Generate env vars (help Julien set them):
```
ssh ubuntu@146.59.233.252 "cd /home/openclaw/leadgen && node -e \"console.log('JWT_SECRET=' + require('crypto').randomBytes(32).toString('hex'))\""
```

2. Prompt Julien to choose a password and generate the hash:
```
ssh ubuntu@146.59.233.252 "cd /home/openclaw/leadgen && node -e \"console.log(require('bcryptjs').hashSync('REPLACE_WITH_PASSWORD', 10))\""
```

3. After env vars are set in `.env`, restart PM2 with a full delete+start (to pick up new env vars -- PM2 caches env on restart):
```
ssh ubuntu@146.59.233.252 "cd /home/openclaw/leadgen && pm2 delete leadgen && pm2 start src/index.js --name leadgen && pm2 save"
```

4. Verify the process is running:
```
ssh ubuntu@146.59.233.252 "pm2 status && pm2 logs leadgen --lines 10 --nostream"
```

IMPORTANT: Do NOT modify any existing cron entries in scheduler.js.
IMPORTANT: Use `pm2 delete + pm2 start` (not `pm2 restart`) to pick up new .env vars.
  </action>
  <verify>
Verify PM2 process is healthy:
```
ssh ubuntu@146.59.233.252 "pm2 status"
```
Verify Express is serving:
```
ssh ubuntu@146.59.233.252 "curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3005/"
```
Should return 200.

Verify port 3005 is bound to 127.0.0.1:
```
ssh ubuntu@146.59.233.252 "ss -tlnp | grep 3005"
```
Should show `127.0.0.1:3005`, NOT `0.0.0.0:3005`.
  </verify>
  <done>
    - Supabase keep-alive cron runs on weekends (Sat/Sun 10:00 Paris)
    - PM2 process running with Express + scheduler
    - Port 3005 bound to 127.0.0.1 (verified with ss)
    - All existing cron tasks still functional
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify HTTPS access and full login flow in browser</name>
  <files>none (verification only)</files>
  <action>
CHECKPOINT: Human verification required. Claude has deployed Express + React + Auth on VPS with port security.

Julien must configure Nginx Proxy Manager and verify the full login flow in browser:

1. Configure Nginx Proxy Manager (Docker UI on VPS):
   - Add Proxy Host: your chosen domain (e.g., leadgen.messagingme.fr)
   - Forward to: 127.0.0.1:3005
   - Enable SSL: Request Let's Encrypt certificate, Force SSL
2. Open https://leadgen.messagingme.fr (or your domain) in browser
3. You should see the login page ("Lead Gen MessagingMe")
4. Log in with your configured email and password
5. You should see the Home page ("Bienvenue")
6. Refresh the browser -- you should still be logged in (JWT persists)
7. Verify port 3005 is NOT accessible from outside: try http://146.59.233.252:3005 in browser -- should fail/timeout
  </action>
  <verify>User confirms all 7 steps pass successfully</verify>
  <done>
    - HTTPS access works through Nginx Proxy Manager
    - Login flow works end-to-end in browser
    - Session persists across refresh
    - Port 3005 not accessible externally
  </done>
</task>

</tasks>

<verification>
Full Phase 4 verification after Nginx setup:

1. INFRA-01: Express serves React SPA and API routes -- `curl http://127.0.0.1:3005/` returns HTML, `/api/auth/login` returns JSON
2. INFRA-02: Port bound to 127.0.0.1 -- `ss -tlnp | grep 3005` shows 127.0.0.1, external access refused
3. INFRA-03: Vite React build in dist/ served by Express -- `ls dist/index.html` exists
4. AUTH-01: Login with email/password works -- POST /api/auth/login returns token
5. AUTH-02: JWT 7-day expiry -- token payload shows correct exp, localStorage persists across refresh
6. AUTH-03: Unauthenticated access redirects to /login -- React auth guard + API 401 handling
</verification>

<success_criteria>
- HTTPS access works through Nginx Proxy Manager on the configured domain
- Port 3005 is NOT accessible from external IPs (bound to 127.0.0.1)
- Full login flow works in browser: login -> home -> refresh -> still logged in
- Supabase keep-alive cron registered for weekends
- PM2 process stable (no restart loops)
- All existing cron tasks still running (scheduler.js unchanged except keep-alive addition)
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-auth-react/04-02-SUMMARY.md`
</output>
