---
phase: 07-settings-export
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/lib/message-generator.js
  - src/tasks/task-a-signals.js
  - src/tasks/task-b-invitations.js
autonomous: true
requirements:
  - CONF-03
  - CONF-05

must_haves:
  truths:
    - "message-generator.js loads template instructions from settings table instead of hardcoded prompts"
    - "task-b-invitations.js reads daily_invitation_limit from settings table with env var fallback"
    - "task-a-signals.js reads daily_lead_limit from settings table with hardcoded fallback"
    - "Changes to templates or limits in the UI take effect on next task run"
  artifacts:
    - path: "src/lib/message-generator.js"
      provides: "Template loading from settings table with fallback to hardcoded defaults"
      contains: "settings"
    - path: "src/tasks/task-b-invitations.js"
      provides: "Dynamic daily invitation limit from settings table"
      contains: "daily_invitation_limit"
    - path: "src/tasks/task-a-signals.js"
      provides: "Dynamic daily lead limit from settings table"
      contains: "daily_lead_limit"
  key_links:
    - from: "src/lib/message-generator.js"
      to: "supabase.from('settings')"
      via: "loadTemplates() query"
      pattern: "supabase\\.from.*settings"
    - from: "src/tasks/task-b-invitations.js"
      to: "supabase.from('settings')"
      via: "loadSetting() query"
      pattern: "settings.*daily_invitation_limit"
---

<objective>
Wire the settings table into existing task code so that UI changes to templates and limits actually take effect at runtime. This completes the backend loop for CONF-03 (limits) and CONF-05 (templates).

Purpose: Without this wiring, editing settings in the UI would have no effect on the actual prospection tasks. This plan makes the settings table the source of truth for configurable values.
Output: Modified message-generator.js, task-a-signals.js, task-b-invitations.js reading from settings table.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-settings-export/07-RESEARCH.md
@.planning/phases/07-settings-export/07-01-SUMMARY.md
@src/lib/message-generator.js
@src/tasks/task-a-signals.js
@src/tasks/task-b-invitations.js
@src/lib/supabase.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire message-generator.js to load templates from settings table</name>
  <files>src/lib/message-generator.js</files>
  <action>
Add a `loadTemplates()` function that queries the settings table for template keys and caches them for the duration of a task run. Pattern: load once per function call, not once per module load (same pattern as `loadIcpRules()` in icp-scorer.js).

Implementation:
1. Add `const { supabase } = require("./supabase");` at top (if not already imported)
2. Create `async function loadTemplates()`:
   - Query `supabase.from('settings').select('key, value').in('key', ['template_invitation', 'template_followup', 'template_email', 'template_whatsapp'])`
   - Convert to object: `Object.fromEntries(data.map(r => [r.key, r.value]))`
   - On error, log warning and return empty object (fail-open: hardcoded defaults will be used)

3. For each of the message generation functions (generateInvitationNote, generateFollowUpMessage, generateEmailMessage, generateWhatsAppMessage):
   - Call `const templates = await loadTemplates()` at the start
   - Use `templates.template_invitation || HARDCODED_DEFAULT` as the instruction text in the Claude prompt
   - Keep current hardcoded text as the fallback default (define as constants at module level)
   - The template text replaces the instruction portion of the system prompt, NOT the entire prompt structure (JSON output format, character limits etc. stay fixed)

4. Extract current hardcoded instruction texts into named constants:
   - `const DEFAULT_INVITATION_TEMPLATE = "...current instruction text..."`
   - `const DEFAULT_FOLLOWUP_TEMPLATE = "..."`
   - `const DEFAULT_EMAIL_TEMPLATE = "..."`
   - `const DEFAULT_WHATSAPP_TEMPLATE = "..."`

Priority chain: settings table value > hardcoded constant default.

IMPORTANT: Do NOT change the function signatures or return types. The rest of the pipeline must work identically. Only the instruction text inside the Claude prompt changes.
  </action>
  <verify>Run `node -e "require('./src/lib/message-generator')"` -- no import errors. Grep for `loadTemplates` and `settings` in the file to confirm wiring.</verify>
  <done>message-generator.js loads template instructions from settings table with graceful fallback to hardcoded defaults. No change to function signatures or output format.</done>
</task>

<task type="auto">
  <name>Task 2: Wire task-a and task-b to read limits from settings table</name>
  <files>src/tasks/task-a-signals.js, src/tasks/task-b-invitations.js</files>
  <action>
Create a shared helper function `loadSetting(key, defaultValue)` or inline the pattern in each task.

**For task-b-invitations.js (daily invitation limit):**

Currently reads `const DAILY_LIMIT = parseInt(process.env.DAILY_INVITATION_LIMIT) || 15;` at module level.

Change to:
1. Add `const { supabase } = require("../lib/supabase");` (if not already imported)
2. Inside the main task function (not at module level), query the setting:
   ```javascript
   let dailyLimit = 15; // default
   try {
     const { data } = await supabase.from('settings').select('value').eq('key', 'daily_invitation_limit').single();
     if (data?.value) dailyLimit = parseInt(data.value) || 15;
   } catch (e) {
     // Fallback to env var then default
     dailyLimit = parseInt(process.env.DAILY_INVITATION_LIMIT) || 15;
   }
   ```
3. Use `dailyLimit` instead of the previous constant throughout the function
4. Priority chain: settings table > env var > hardcoded 15

**For task-a-signals.js (daily lead limit):**

Currently has a hardcoded `50` for daily lead cap (likely in a `.limit(50)` or similar).

Change to:
1. Add supabase import if not present
2. Inside the main task function, query the setting:
   ```javascript
   let dailyLeadLimit = 50; // default
   try {
     const { data } = await supabase.from('settings').select('value').eq('key', 'daily_lead_limit').single();
     if (data?.value) dailyLeadLimit = parseInt(data.value) || 50;
   } catch (e) {
     dailyLeadLimit = 50;
   }
   ```
3. Use `dailyLeadLimit` instead of the hardcoded 50

IMPORTANT: Both changes must be inside the async task function, not at module level, so they pick up the latest value on each run. Keep the existing try/catch error isolation pattern.
  </action>
  <verify>Run `node -e "require('./src/tasks/task-a-signals')"` and `node -e "require('./src/tasks/task-b-invitations')"` -- no import errors. Grep for `settings` in both files to confirm wiring.</verify>
  <done>Both tasks read their respective limits from the settings table at runtime, with fallback chain to env vars and hardcoded defaults. Changes in the UI take effect on next task run.</done>
</task>

</tasks>

<verification>
1. `node -e "require('./src/lib/message-generator')"` loads without error
2. `node -e "require('./src/tasks/task-a-signals')"` loads without error
3. `node -e "require('./src/tasks/task-b-invitations')"` loads without error
4. `grep -l "settings" src/lib/message-generator.js src/tasks/task-a-signals.js src/tasks/task-b-invitations.js` returns all 3 files
</verification>

<success_criteria>
- message-generator.js loads templates from settings table and falls back to hardcoded defaults
- task-b reads daily invitation limit from settings table
- task-a reads daily lead limit from settings table
- All three files load without import errors
- No change to function signatures or task behavior when settings table has default values
</success_criteria>

<output>
After completion, create `.planning/phases/07-settings-export/07-03-SUMMARY.md`
</output>
