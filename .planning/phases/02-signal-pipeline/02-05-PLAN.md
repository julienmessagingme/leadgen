---
phase: 02-signal-pipeline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/icp-scorer.js]
autonomous: true
gap_closure: true
requirements: [ICP-01]

must_haves:
  truths:
    - "Claude Haiku returns structured JSON with icp_score, tier, reasoning fields"
    - "JSON.parse on Claude response never throws SyntaxError in normal operation"
    - "Leads are scored with real numeric scores (not all 0 from fail-safe)"
  artifacts:
    - path: "src/lib/icp-scorer.js"
      provides: "ICP scoring with guaranteed JSON output from Claude Haiku"
      contains: "anthropic.beta.messages.create"
  key_links:
    - from: "src/lib/icp-scorer.js"
      to: "Anthropic beta API"
      via: "anthropic.beta.messages.create with output_config"
      pattern: "anthropic\\.beta\\.messages\\.create"
---

<objective>
Fix the Anthropic API call in icp-scorer.js to use the beta path required for structured JSON output.

Purpose: The `output_config` parameter (json_schema) is only recognized by `anthropic.beta.messages.create`, not the standard `anthropic.messages.create`. Without this fix, Claude Haiku responds in prose, `JSON.parse` throws, and the catch block assigns cold tier + score 0 to every lead — effectively disabling ICP scoring.

Output: Fixed icp-scorer.js that guarantees structured JSON responses from Claude Haiku.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signal-pipeline/02-VERIFICATION.md
@.planning/phases/02-signal-pipeline/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Anthropic API path from standard to beta for structured output</name>
  <files>src/lib/icp-scorer.js</files>
  <action>
    In src/lib/icp-scorer.js, change the Anthropic API call at line ~127 from:

    `anthropic.messages.create({...})`

    to:

    `anthropic.beta.messages.create({...})`

    This is a one-line change. The `output_config` block with `json_schema` format (lines 131-145) is already correct — it just needs the beta API path to be recognized.

    Do NOT change the model, max_tokens, messages, or output_config parameters. Only change the API path.

    After the change, verify that the rest of the scoring pipeline (JSON.parse on line 148, haikuScore extraction on line 149) remains unchanged, as they already expect the correct JSON structure.
  </action>
  <verify>
    1. Run: `grep -n "anthropic.beta.messages.create" src/lib/icp-scorer.js` — must match exactly one line
    2. Run: `grep -n "anthropic.messages.create" src/lib/icp-scorer.js` — must return NO results (the old standard path must be gone)
    3. Run: `node -e "require('./src/lib/icp-scorer.js')"` with ANTHROPIC_API_KEY set — module loads without syntax errors
  </verify>
  <done>
    icp-scorer.js calls anthropic.beta.messages.create with output_config json_schema. The standard anthropic.messages.create path is no longer used. Module loads cleanly.
  </done>
</task>

</tasks>

<verification>
- `anthropic.beta.messages.create` appears in icp-scorer.js (grep confirms)
- `anthropic.messages.create` does NOT appear in icp-scorer.js (no standard path left)
- The output_config block with json_schema format is preserved intact
- The JSON.parse and scoring pipeline downstream of the API call is unchanged
- Module requires cleanly (no syntax errors)
</verification>

<success_criteria>
The ICP scoring module uses the correct Anthropic beta API path, ensuring that output_config with json_schema is recognized by the SDK. Claude Haiku will return structured JSON responses, and JSON.parse will succeed in normal operation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-signal-pipeline/02-05-SUMMARY.md`
</output>
