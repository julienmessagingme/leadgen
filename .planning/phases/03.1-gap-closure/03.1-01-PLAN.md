---
phase: 03.1-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/openclaw/leadgen/src/lib/icp-scorer.js
  - /home/openclaw/leadgen/src/db/migrations/001_initial_schema.sql
  - /home/openclaw/leadgen/src/scheduler.js
  - .planning/phases/01-foundation/01-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [ICP-01, ICP-05, LIN-07, WA-01, WA-02, INMAIL-03, INFRA-01, INFRA-02, INFRA-03, INFRA-04, LOG-01, LOG-02, LOG-03]

must_haves:
  truths:
    - "ICP scorer on VPS uses beta.messages.create (not messages.create) so output_config json_schema works"
    - "Supabase leads table has follow_up_sent_at column so task-c-followup.js can track sent follow-ups"
    - "Supabase leads table has whatsapp_template_created_at column so task-e-whatsapp.js can track templates"
    - "WhatsApp poll cron covers 9h-18h (not 9h-17h59) so last poll fires at 18:00"
    - "INMAIL-03 marked complete in REQUIREMENTS.md (email delivery is accepted implementation)"
    - "Phase 1 has a VERIFICATION.md confirming all 7 foundation requirements"
  artifacts:
    - path: "/home/openclaw/leadgen/src/lib/icp-scorer.js"
      provides: "ICP scoring with correct Anthropic beta API path"
      contains: "beta.messages.create"
    - path: "/home/openclaw/leadgen/src/scheduler.js"
      provides: "Fixed WhatsApp poll cron expression"
      contains: "*/15 9-18 * * 1-5"
    - path: ".planning/phases/01-foundation/01-VERIFICATION.md"
      provides: "Retroactive Phase 1 verification"
  key_links:
    - from: "src/lib/icp-scorer.js"
      to: "Anthropic beta API"
      via: "beta.messages.create with output_config"
      pattern: "beta\\.messages\\.create"
    - from: "src/scheduler.js"
      to: "WhatsApp poll task"
      via: "cron expression"
      pattern: "9-18"
---

<objective>
Fix 3 critical runtime bugs (ICP scorer beta API path, 2 missing DB columns), 1 minor cron timing issue, align INMAIL-03, and create retroactive Phase 1 verification.

Purpose: Close all gaps identified in v1.0-MILESTONE-AUDIT.md so the pipeline is fully operational.
Output: All 3 broken E2E flows restored, Phase 1 verified, milestone v1.0 closable.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix icp-scorer beta API + add missing DB columns + fix WA-02 cron</name>
  <files>
    /home/openclaw/leadgen/src/lib/icp-scorer.js
    /home/openclaw/leadgen/src/db/migrations/001_initial_schema.sql
    /home/openclaw/leadgen/src/scheduler.js
  </files>
  <action>
All work via SSH: `ssh ubuntu@146.59.233.252`

**[P0] Fix icp-scorer.js beta API path:**
1. Edit `/home/openclaw/leadgen/src/lib/icp-scorer.js` line ~127
2. Change `getAnthropicClient().messages.create` to `getAnthropicClient().beta.messages.create`
3. Verify: `grep -n "beta.messages.create" /home/openclaw/leadgen/src/lib/icp-scorer.js` should match

**[P0] Add missing DB columns to Supabase:**
1. Run SQL against Supabase (use the psql connection or supabase client):
```sql
ALTER TABLE leads ADD COLUMN IF NOT EXISTS follow_up_sent_at timestamptz;
ALTER TABLE leads ADD COLUMN IF NOT EXISTS whatsapp_template_created_at timestamptz;
```
2. Also update `/home/openclaw/leadgen/src/db/migrations/001_initial_schema.sql` — add both columns to the leads CREATE TABLE block for schema consistency.

**[P2] Fix WA-02 cron timing:**
1. Edit `/home/openclaw/leadgen/src/scheduler.js`
2. Find the whatsapp-poll cron expression `*/15 9-17 * * 1-5` and change to `*/15 9-18 * * 1-5`

**Restart PM2:**
```bash
cd /home/openclaw/leadgen && pm2 restart leadgen
```

**Commit on VPS:**
```bash
cd /home/openclaw/leadgen && git add src/lib/icp-scorer.js src/db/migrations/001_initial_schema.sql src/scheduler.js && git commit -m "fix(03.1): beta API path, missing DB columns, WA-02 cron 9-18h"
```
  </action>
  <verify>
Via SSH:
1. `grep -n "beta.messages.create" /home/openclaw/leadgen/src/lib/icp-scorer.js` — must match
2. `grep -n "follow_up_sent_at\|whatsapp_template_created_at" /home/openclaw/leadgen/src/db/migrations/001_initial_schema.sql` — both columns present
3. `grep -n "9-18" /home/openclaw/leadgen/src/scheduler.js` — whatsapp-poll cron updated
4. `pm2 status` — leadgen process is online
5. Test DB columns exist: run a quick query via the app or psql to confirm columns are accessible
  </verify>
  <done>
- icp-scorer.js uses beta.messages.create on VPS (ICP-01, ICP-05 fixed)
- follow_up_sent_at column exists in Supabase leads table (LIN-07, WA-01 unblocked)
- whatsapp_template_created_at column exists in Supabase leads table (WA-01 fixed)
- WhatsApp poll cron covers 9h-18h (WA-02 fixed)
- PM2 process restarted and online
  </done>
</task>

<task type="auto">
  <name>Task 2: Align INMAIL-03 + create Phase 1 retroactive VERIFICATION.md</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/phases/01-foundation/01-VERIFICATION.md
  </files>
  <action>
**Mark INMAIL-03 complete in REQUIREMENTS.md:**
1. In `.planning/REQUIREMENTS.md`, change INMAIL-03 line from `- [ ]` to `- [x]`
2. The requirement text already reflects email delivery (updated during audit). Just check the box.
3. In the Traceability table, change INMAIL-03 from `Pending` to `Complete` and phase from `Phase 3.1` to `Phase 3`

**Also update ICP-01, ICP-05, LIN-07, WA-01, WA-02 status in Traceability table:**
- ICP-01: Phase 3.1 → Complete
- ICP-05: Phase 3.1 → Complete
- LIN-07: Phase 3.1 → Complete
- WA-01: Phase 3.1 → Complete
- WA-02: Phase 3.1 → Complete
- INMAIL-03: Phase 3.1 → Complete

**Create retroactive Phase 1 VERIFICATION.md:**
Create `.planning/phases/01-foundation/01-VERIFICATION.md` documenting that all 7 Phase 1 requirements (INFRA-01 through INFRA-04, LOG-01 through LOG-03) are verified. Evidence:
- INFRA-01: VPS running Node.js 20+ and Python in /home/openclaw/leadgen/ — confirmed by Phases 2+3 execution
- INFRA-02: Scheduler node-cron with 7 cron entries Mon-Fri — confirmed by 03-05-SUMMARY
- INFRA-03: Supabase schema with 8 tables, ENUMs, RLS — confirmed by all task queries
- INFRA-04: OpenClaw configured with env vars — confirmed by 02-04 Sales Nav integration
- LOG-01: Logs table with run_id tracking — confirmed by all task executions
- LOG-02: Error isolation per task — confirmed by registerTask wrapper in scheduler
- LOG-03: RGPD suppression_list with SHA256 — confirmed by outreach tasks checking suppression

Use frontmatter format:
```yaml
---
phase: 01-foundation
status: verified_retroactive
verified: {today's date}
requirements_verified: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, LOG-01, LOG-02, LOG-03]
all_passed: true
method: retroactive (downstream phases confirm Phase 1 artifacts)
---
```

Also update INFRA-01-04 and LOG-01-03 in the Traceability table from `Complete` to `Complete` (they should already be marked complete — just verify).
  </action>
  <verify>
1. `grep "INMAIL-03" .planning/REQUIREMENTS.md` — shows `[x]` and `Complete`
2. `cat .planning/phases/01-foundation/01-VERIFICATION.md` — exists with all 7 requirements listed as verified
3. All 13 requirement IDs from this phase appear as Complete in REQUIREMENTS.md traceability table
  </verify>
  <done>
- INMAIL-03 marked complete (email delivery accepted as implementation)
- All 6 gap-closure requirements (ICP-01, ICP-05, LIN-07, WA-01, WA-02, INMAIL-03) marked complete in traceability
- Phase 1 VERIFICATION.md exists with retroactive verification of INFRA-01-04 and LOG-01-03
- All 13 phase 3.1 requirement IDs resolved
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 3 previously broken E2E flows should be unblocked:
   - Signal → Score → Insert: beta.messages.create fixes ICP scoring
   - LinkedIn Follow-up: follow_up_sent_at column exists
   - WhatsApp Template: whatsapp_template_created_at column exists
2. WhatsApp poll covers full 9h-18h window
3. All 13 requirement IDs from phase 3.1 are resolved
4. Phase 1 has VERIFICATION.md
</verification>

<success_criteria>
- `grep "beta.messages.create" /home/openclaw/leadgen/src/lib/icp-scorer.js` matches
- Both new columns queryable in Supabase leads table
- Cron expression reads `*/15 9-18 * * 1-5`
- PM2 process online after restart
- 01-VERIFICATION.md exists in phases/01-foundation/
- All 13 requirement IDs marked Complete in REQUIREMENTS.md traceability
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-gap-closure/03.1-01-SUMMARY.md`
</output>
