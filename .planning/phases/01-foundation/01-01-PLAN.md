---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/openclaw/leadgen/package.json
  - /home/openclaw/leadgen/.env
  - /home/openclaw/leadgen/.env.example
  - /home/openclaw/leadgen/.gitignore
  - /home/openclaw/leadgen/src/index.js
  - /home/openclaw/leadgen/src/lib/supabase.js
autonomous: false
requirements: [INFRA-01, INFRA-04]
user_setup:
  - service: supabase
    why: "Supabase project credentials needed for .env"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon public"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role secret"
  - service: openclaw
    why: "OpenClaw browser automation credentials for Sales Navigator"
    env_vars:
      - name: OPENCLAW_API_KEY
        source: "OpenClaw installation/config on VPS"

must_haves:
  truths:
    - "Le repertoire /home/openclaw/leadgen/ existe avec Node.js 20+ installe via nvm"
    - "npm install fonctionne et les dependances sont installees"
    - "OpenClaw est installe et accessible sous /home/openclaw/"
    - "Le fichier .env contient les variables Supabase et OpenClaw, et le process valide leur presence au demarrage"
    - "Le client Supabase se connecte avec la service_role key et repond a une requete test"
  artifacts:
    - path: "/home/openclaw/leadgen/package.json"
      provides: "Node.js project with dependencies"
      contains: "node-cron"
    - path: "/home/openclaw/leadgen/src/index.js"
      provides: "Entry point with env validation"
      contains: "SUPABASE_URL"
    - path: "/home/openclaw/leadgen/src/lib/supabase.js"
      provides: "Supabase client singleton"
      exports: ["supabase"]
  key_links:
    - from: "/home/openclaw/leadgen/src/index.js"
      to: "/home/openclaw/leadgen/.env"
      via: "dotenv config loading"
      pattern: "require.*dotenv.*config"
    - from: "/home/openclaw/leadgen/src/lib/supabase.js"
      to: "Supabase external project"
      via: "createClient with service_role key"
      pattern: "createClient.*SUPABASE_URL"
---

<objective>
Initialiser le projet Node.js sur le VPS et configurer la connexion Supabase.

Purpose: Creer la base du projet dans /home/openclaw/leadgen/ avec toutes les dependances, la gestion securisee des variables d'environnement, et le client Supabase pret a etre utilise par les plans suivants.
Output: Projet Node.js fonctionnel avec client Supabase connecte.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Node.js project on VPS</name>
  <files>
    /home/openclaw/leadgen/package.json
    /home/openclaw/leadgen/.gitignore
  </files>
  <action>
SSH into the VPS (root@146.59.233.252) and execute:

1. Create project directory: `mkdir -p /home/openclaw/leadgen/src/{tasks,lib,db/migrations} /home/openclaw/leadgen/scripts /home/openclaw/leadgen/logs`
2. Verify Node.js 20+ is available: `node --version`. If not installed or < 20, install via nvm:
   - `curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash`
   - `source ~/.bashrc && nvm install 20`
   - `npm install -g pm2`
3. In /home/openclaw/leadgen/, run `npm init -y`
4. Install dependencies: `npm install node-cron @supabase/supabase-js dotenv`
5. Create .gitignore with: `node_modules/`, `.env`, `logs/`
6. Verify Python 3.11+ is available: `python3 --version`. If not, install: `apt install -y python3.11` (or via deadsnakes PPA if Ubuntu < 22.04)
7. Install OpenClaw under /home/openclaw/:
   - Check if OpenClaw is already installed: `which openclaw || ls /home/openclaw/openclaw* 2>/dev/null`
   - If not installed, install it (pip-based): `pip3 install openclaw` or clone from source if private repo
   - Verify: `openclaw --version` or `python3 -c "import openclaw; print(openclaw.__version__)"`
   - OpenClaw is used for browser automation (Sales Navigator scraping in Phase 2)

IMPORTANT: Do NOT touch /home/keolis/ or /home/educnat/ directories. Do NOT bind to ports 3000 or 3002.
  </action>
  <verify>
SSH and run:
- `node --version` shows v20+
- `python3 --version` shows 3.11+
- `ls /home/openclaw/leadgen/node_modules/@supabase/supabase-js` exists
- `cat /home/openclaw/leadgen/package.json` shows all dependencies
- `pm2 --version` returns a version
- `openclaw --version` or `python3 -c "import openclaw"` succeeds
  </verify>
  <done>Node.js 20+ and Python 3.11+ installed, OpenClaw installed, project initialized with all dependencies in /home/openclaw/leadgen/</done>
</task>

<task type="auto">
  <name>Task 2: Create env config and Supabase client</name>
  <files>
    /home/openclaw/leadgen/.env
    /home/openclaw/leadgen/.env.example
    /home/openclaw/leadgen/src/index.js
    /home/openclaw/leadgen/src/lib/supabase.js
  </files>
  <action>
1. Create `/home/openclaw/leadgen/.env.example` with all required variables (no secrets):
```
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NODE_ENV=production
OPENCLAW_API_KEY=
OPENCLAW_BROWSER_PATH=
OPENCLAW_HEADLESS=true
```

2. Create `/home/openclaw/leadgen/.env` with the same structure. User must fill in Supabase credentials from their dashboard.

3. Create `/home/openclaw/leadgen/src/lib/supabase.js`:
   - Load dotenv if not already loaded
   - Create and export a Supabase client using `createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY)`
   - Use service_role key (server-side process, bypasses RLS)

4. Create `/home/openclaw/leadgen/src/index.js`:
   - Load dotenv at the very top: `require('dotenv').config()`
   - Define REQUIRED_VARS array: `['SUPABASE_URL', 'SUPABASE_SERVICE_ROLE_KEY', 'SUPABASE_ANON_KEY', 'OPENCLAW_API_KEY']`
   - Loop and check each var, `process.exit(1)` with clear error message if any missing
   - After validation, log "Environment validated" and require('./scheduler') (scheduler will be created in Plan 03)
   - For now, add a placeholder: `console.log('Scheduler not yet implemented — Phase 1 Plan 03')`

Use CommonJS (require/module.exports), not ESM. This follows the research recommendation.
  </action>
  <verify>
SSH and run:
- `node -e "require('dotenv').config(); console.log(process.env.SUPABASE_URL ? 'OK' : 'MISSING')"` from /home/openclaw/leadgen/
- `node src/index.js` — should either validate env or exit with clear error about missing vars
- `node -e "const { supabase } = require('./src/lib/supabase'); supabase.from('logs').select('*').limit(1).then(r => console.log(r.error ? 'ERROR: ' + r.error.message : 'CONNECTED'))"` from /home/openclaw/leadgen/
  </verify>
  <done>.env file exists with Supabase credentials, index.js validates all env vars at startup, supabase.js exports a working client singleton</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Supabase connection</name>
  <files>N/A — verification only</files>
  <action>
Human verification checkpoint. Verify that the VPS setup and Supabase connection work correctly.

What was built: Node.js project initialized on VPS with Supabase client configured.

Steps to verify:
1. SSH into VPS: `ssh root@146.59.233.252`
2. Navigate: `cd /home/openclaw/leadgen`
3. Run: `node src/index.js` — should print "Environment validated" (not crash with missing env vars)
4. Verify Supabase connectivity: `node -e "const { supabase } = require('./src/lib/supabase'); supabase.from('logs').select('count').then(r => console.log(r))"`
5. Verify Keolis still works: `curl -s http://localhost:3000 | head -5` should respond normally
  </action>
  <verify>User confirms Supabase connects and Keolis is unaffected</verify>
  <done>User approved: VPS project setup works, Supabase connects, existing services unaffected</done>
</task>

</tasks>

<verification>
- Node.js 20+ and Python 3.11+ available on VPS
- Project structure exists in /home/openclaw/leadgen/
- All npm dependencies installed
- .env contains valid Supabase credentials
- Supabase client connects successfully
- Existing services (Keolis, Educnat) unaffected
</verification>

<success_criteria>
- `node --version` returns v20.x+
- `python3 --version` returns 3.11+
- `node src/index.js` validates env and starts without error
- Supabase client can execute a test query
- Ports 3000/3002 still respond to Keolis
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
