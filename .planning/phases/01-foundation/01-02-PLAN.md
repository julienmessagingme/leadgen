---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - /home/openclaw/leadgen/src/db/migrations/001_initial_schema.sql
  - /home/openclaw/leadgen/scripts/deploy-schema.js
  - /home/openclaw/leadgen/scripts/seed.js
autonomous: true
requirements: [INFRA-03]

must_haves:
  truths:
    - "Les 8 tables existent dans Supabase et acceptent des INSERT/SELECT"
    - "Les 4 ENUM types (lead_status, lead_tier, signal_type, signal_category) sont crees"
    - "RLS est active sur toutes les tables"
    - "Les seed data sont presentes dans icp_rules et global_settings"
    - "Les index sont crees pour les colonnes de recherche frequentes"
  artifacts:
    - path: "/home/openclaw/leadgen/src/db/migrations/001_initial_schema.sql"
      provides: "Complete DDL for all 8 tables, ENUMs, indexes, RLS"
      contains: "CREATE TABLE leads"
    - path: "/home/openclaw/leadgen/scripts/deploy-schema.js"
      provides: "Script to execute migration SQL against Supabase"
      contains: "supabase"
    - path: "/home/openclaw/leadgen/scripts/seed.js"
      provides: "Seed data insertion for icp_rules and global_settings"
      contains: "icp_rules"
  key_links:
    - from: "/home/openclaw/leadgen/scripts/deploy-schema.js"
      to: "Supabase SQL editor"
      via: "Direct SQL execution via Supabase client or REST API"
      pattern: "supabase.*rpc|sql"
---

<objective>
Deployer le schema complet Supabase (8 tables, ENUMs, RLS, indexes, seed data).

Purpose: Creer la structure de donnees complete dans Supabase pour que les plans suivants puissent ecrire/lire les leads, logs, et configurations.
Output: Schema Supabase deploye et verifie avec donnees de seed.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and deploy Supabase schema migration</name>
  <files>
    /home/openclaw/leadgen/src/db/migrations/001_initial_schema.sql
    /home/openclaw/leadgen/scripts/deploy-schema.js
  </files>
  <action>
1. Create `/home/openclaw/leadgen/src/db/migrations/001_initial_schema.sql` with the COMPLETE schema from the research document (section "Supabase Schema Migration"). The SQL must follow this exact order:
   - 4 ENUM types: lead_status, lead_tier, signal_type, signal_category
   - 8 tables: sequences, watchlist, icp_rules, leads, logs, global_settings, suppression_list, lead_news_evidence
   - All indexes (idx_leads_canonical, idx_leads_status, idx_leads_tier, idx_leads_sequence, idx_leads_signal_date, idx_logs_run_id, idx_logs_task, idx_logs_created, idx_suppression_hash, idx_watchlist_active, idx_news_lead)
   - RLS enable on all 8 tables
   - RLS policies for authenticated role (read on all, write on watchlist/icp_rules/sequences/global_settings/suppression_list)

   Use the exact SQL from the research 01-RESEARCH.md "Supabase Schema Migration (INFRA-03)" section. Key schema rules per Supabase best practices:
   - `bigint GENERATED ALWAYS AS IDENTITY` for PKs (not serial)
   - `text` not `varchar(n)`
   - `timestamptz` not `timestamp`
   - Wrap RLS auth functions in `(select ...)` subqueries for performance

2. Create `/home/openclaw/leadgen/scripts/deploy-schema.js`:
   - Load dotenv and Supabase client
   - Read the SQL file from disk
   - Execute via Supabase SQL: use `supabase.rpc('exec_sql', { sql })` or, if that's not available, guide the user to paste SQL into Supabase Dashboard SQL Editor
   - Alternative approach: use the Supabase Management API or simply provide instructions to run via `psql` or the SQL Editor
   - The script should handle the case where types/tables already exist gracefully (use IF NOT EXISTS where possible, or CREATE OR REPLACE)
   - Print success/failure for each major step

NOTE: Supabase free tier does not expose raw SQL execution via the JS client. The recommended approach is:
   - Option A: Execute SQL via the Supabase Dashboard SQL Editor (paste the .sql file)
   - Option B: Use `psql` with the Supabase connection string
   - The deploy-schema.js should detect which method is available and guide accordingly. At minimum, it should read the SQL, print it, and provide the connection instructions.
  </action>
  <verify>
After deploying (via SQL Editor or psql):
- Query each table: `SELECT count(*) FROM sequences;` (should return 0)
- Verify ENUMs: `SELECT enum_range(NULL::lead_status);` should list all values
- Verify RLS: `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public';` — all 8 tables should show rowsecurity = true
- Verify indexes: `SELECT indexname FROM pg_indexes WHERE schemaname = 'public';` — should list all 11 indexes
  </verify>
  <done>All 8 tables, 4 ENUMs, 11 indexes, and RLS policies exist in Supabase and respond to queries</done>
</task>

<task type="auto">
  <name>Task 2: Insert seed data</name>
  <files>
    /home/openclaw/leadgen/scripts/seed.js
  </files>
  <action>
1. Create `/home/openclaw/leadgen/scripts/seed.js`:
   - Load dotenv and require the Supabase client from `../src/lib/supabase.js`
   - Insert seed data into `icp_rules` table (6 rows):
     - signal_weights: `{"concurrent": 25, "influenceur": 15, "sujet": 10, "job": 5}`, weight 100
     - titles: `{"positive": ["directeur", "head of", "VP", "responsable", "chief"], "negative": ["stagiaire", "intern", "student"]}`, weight 20
     - sectors: `{"positive": ["retail", "e-commerce", "telecom", "banque", "assurance"], "negative": []}`, weight 15
     - sizes: `{"min": 50, "max": 10000}`, weight 10
     - seniority: `{"min_years": 2}`, weight 10
     - freshness: `{"warn_days": 5, "malus_days": 10, "skip_days": 15}`, weight 100
   - Insert seed data into `global_settings` table (7 rows):
     - daily_invitation_limit: 15
     - daily_lead_limit: 50
     - linkedin_delay_min_ms: 60000
     - linkedin_delay_max_ms: 120000
     - bereach_cache_hours: 48
     - calendly_url: "https://calendly.com/julien-messagingme"
     - whatsapp_workspace_id: "185117"
   - Use `upsert` with `onConflict` to make the script idempotent (safe to re-run)
   - Print results: "Inserted N icp_rules, M global_settings"

2. Run the seed script: `node scripts/seed.js`
  </action>
  <verify>
Run from /home/openclaw/leadgen/:
- `node -e "const {supabase}=require('./src/lib/supabase'); supabase.from('icp_rules').select('category').then(r=>console.log(r.data.length, 'rules'));"` — should print "6 rules"
- `node -e "const {supabase}=require('./src/lib/supabase'); supabase.from('global_settings').select('key').then(r=>console.log(r.data.length, 'settings'));"` — should print "7 settings"
  </verify>
  <done>6 icp_rules and 7 global_settings rows exist in Supabase, seed script is idempotent</done>
</task>

</tasks>

<verification>
- All 8 tables exist and accept queries
- 4 ENUM types created correctly
- RLS enabled on all tables
- 11 indexes created
- Seed data present in icp_rules (6 rows) and global_settings (7 rows)
- Schema matches the specification from 01-RESEARCH.md exactly
</verification>

<success_criteria>
- `SELECT count(*) FROM leads;` returns 0 (table exists)
- `SELECT count(*) FROM icp_rules;` returns 6
- `SELECT count(*) FROM global_settings;` returns 7
- All ENUM values accessible
- RLS active on all 8 tables
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
