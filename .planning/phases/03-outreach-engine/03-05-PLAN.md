---
phase: 03-outreach-engine
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - src/scheduler.js
  - src/index.js
autonomous: false
requirements: [LIN-01, LIN-03, LIN-06, EMAIL-06, WA-02, WA-03, INMAIL-03]

must_haves:
  truths:
    - "Scheduler registers all 5 outreach tasks (B-F) + WhatsApp polling at correct times"
    - "Task B runs at 09h00 Mon-Fri, Task C at 11h00, Task D at 10h00, Task E at 10h30, Task F at 08h30"
    - "WhatsApp polling runs every 15 min Mon-Fri 9h-18h"
    - "PM2 restarts the process with all tasks active"
    - "New env vars validated at startup (GMAIL_USER, MESSAGINGME_API_KEY, etc.)"
  artifacts:
    - path: "src/scheduler.js"
      provides: "Updated scheduler with all outreach task registrations"
      contains: "task-b-invitations"
    - path: "src/index.js"
      provides: "Updated env validation with new required vars"
  key_links:
    - from: "src/scheduler.js"
      to: "src/tasks/task-b-invitations.js"
      via: "registerTask('task-b-invitations', '0 9 * * 1-5', taskBInvitations)"
      pattern: "task-b-invitations"
    - from: "src/scheduler.js"
      to: "src/tasks/task-e-whatsapp.js"
      via: "registerTask('task-e-whatsapp', '30 10 * * 1-5', taskEWhatsapp)"
      pattern: "task-e-whatsapp"
    - from: "src/scheduler.js"
      to: "src/tasks/whatsapp-poll.js"
      via: "registerTask('whatsapp-poll', '*/15 9-17 * * 1-5', whatsappPoll)"
      pattern: "whatsapp-poll"

user_setup:
  - service: gmail
    why: "Email relance J+7 via SMTP"
    env_vars:
      - name: GMAIL_USER
        source: "julien@messagingme.fr (already known)"
      - name: GMAIL_APP_PASSWORD
        source: "Google Account -> Security -> 2-Step Verification -> App passwords -> Generate new"
    dashboard_config: []
  - service: messagingme
    why: "WhatsApp template creation and sending"
    env_vars:
      - name: MESSAGINGME_API_KEY
        source: "MessagingMe Dashboard -> API Settings (already known: SltDYkJjGwq0CZizgv0JmdwFrKoy8RT7XreLDKB2dG1rI8Cc3RqJxKkA2ykc)"
      - name: MESSAGINGME_WORKSPACE_ID
        source: "MessagingMe Dashboard -> Workspace Settings (already known: 185117)"
      - name: MESSAGINGME_TEMPLATE_NAMESPACE
        source: "MessagingMe Dashboard -> WhatsApp Templates -> any template -> namespace field"
    dashboard_config:
      - task: "Create utility template 'daily_leadgen_briefing' with single {{1}} body parameter"
        location: "MessagingMe Dashboard -> WhatsApp Templates -> Create Template"
  - service: calendly
    why: "CTA link in outreach messages"
    env_vars:
      - name: CALENDLY_URL
        source: "Calendly dashboard -> Event page URL"
    dashboard_config: []
  - service: whatsapp
    why: "Julien's phone for receiving briefings and alerts"
    env_vars:
      - name: JULIEN_WHATSAPP_PHONE
        source: "Julien's WhatsApp phone number in international format (+33...)"
    dashboard_config: []
---

<objective>
Wire all outreach tasks into the scheduler, update environment validation, restart PM2, and verify the complete outreach engine is operational.

Purpose: Plans 02-04 created the task modules but they are not yet registered in the scheduler or started. This plan connects everything and validates the full pipeline.
Output: All outreach tasks running on schedule via PM2.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-outreach-engine/03-RESEARCH.md
@.planning/phases/03-outreach-engine/03-01-SUMMARY.md
@.planning/phases/03-outreach-engine/03-02-SUMMARY.md
@.planning/phases/03-outreach-engine/03-03-SUMMARY.md
@.planning/phases/03-outreach-engine/03-04-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update scheduler, env validation, and restart PM2</name>
  <files>src/scheduler.js, src/index.js</files>
  <action>
**Update src/scheduler.js on VPS:**
The scheduler already has placeholder registrations for tasks B-F from Phase 1 (01-03-SUMMARY confirms all 7 cron entries exist). The placeholders are module.exports stubs. Since plans 02-04 replaced the placeholder files with real implementations, the scheduler imports should already point to the right files.

Verify and update if needed:
- `task-b-invitations` registered at `0 9 * * 1-5` (09h00 Mon-Fri Europe/Paris)
- `task-c-followup` registered at `0 11 * * 1-5` (11h00 Mon-Fri)
- `task-d-email` registered at `0 10 * * 1-5` (10h00 Mon-Fri)
- `task-e-whatsapp` registered at `30 10 * * 1-5` (10h30 Mon-Fri)
- `task-f-briefing` registered at `30 8 * * 1-5` (08h30 Mon-Fri)
- `whatsapp-poll` registered at `*/15 9-17 * * 1-5` (every 15 min, 9h-18h Mon-Fri)

If the scheduler already uses registerTask for all these, no changes needed -- the real implementations will be picked up automatically since the placeholder files were replaced.

**Update src/index.js on VPS:**
Follow the research recommendation: keep lazy init in each module. Do NOT add outreach vars to the required startup list. Instead, add them to a RECOMMENDED_VARS array that logs warnings but does not exit. This way PM2 can start the process even if Gmail is not configured yet.

Add a RECOMMENDED_VARS array with: GMAIL_USER, GMAIL_APP_PASSWORD, MESSAGINGME_API_KEY, MESSAGINGME_WORKSPACE_ID, JULIEN_WHATSAPP_PHONE, CALENDLY_URL. At startup, check each and log a warning (console.warn) if missing, but do NOT process.exit.

**Add .env vars on VPS:**
SSH to VPS and add the new variables to `/home/openclaw/leadgen/.env`:
```
GMAIL_USER=julien@messagingme.fr
GMAIL_APP_PASSWORD=
MESSAGINGME_API_KEY=SltDYkJjGwq0CZizgv0JmdwFrKoy8RT7XreLDKB2dG1rI8Cc3RqJxKkA2ykc
MESSAGINGME_WORKSPACE_ID=185117
JULIEN_WHATSAPP_PHONE=
CALENDLY_URL=
MESSAGINGME_TEMPLATE_NAMESPACE=
DAILY_INVITATION_LIMIT=15
```

**Restart PM2:**
```bash
cd /home/openclaw/leadgen
pm2 restart openclaw-leadgen
pm2 logs openclaw-leadgen --lines 20
```

Verify the process starts without crashing. If it crashes due to missing env vars, check which var and update the warning-only approach.
  </action>
  <verify>
On VPS:
- `pm2 status` shows openclaw-leadgen as 'online'
- `pm2 logs openclaw-leadgen --lines 30` shows no crash, warnings for unconfigured optional vars are OK
- `cat /home/openclaw/leadgen/.env | grep -c "GMAIL\|MESSAGINGME\|JULIEN\|CALENDLY"` shows 7 new lines
- Scheduler registers all 7 cron entries (check logs for "Registered task" messages)
  </verify>
  <done>All outreach tasks registered in scheduler, env vars added to .env (some pending user fill-in), PM2 restarted with process online</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete outreach engine and configure external services</name>
  <files>src/scheduler.js</files>
  <action>
Human verification of the complete Phase 3 Outreach Engine: 5 task modules (B-F + WhatsApp polling), 3 new lib modules (gmail, messagingme, message-generator), 2 extended lib modules (bereach, hubspot), all wired into scheduler and running via PM2.

Steps for user:
1. SSH to VPS: `ssh ubuntu@146.59.233.252`
2. Check PM2 status: `pm2 status` -- openclaw-leadgen should be 'online'
3. Check PM2 logs: `pm2 logs openclaw-leadgen --lines 50` -- should show task registrations, no crashes
4. Fill in missing .env values:
   - GMAIL_APP_PASSWORD: Generate at Google Account -> Security -> 2-Step Verification -> App passwords
   - JULIEN_WHATSAPP_PHONE: Your WhatsApp phone in +33 format
   - CALENDLY_URL: Your Calendly booking page URL
   - MESSAGINGME_TEMPLATE_NAMESPACE: From MessagingMe dashboard
5. Create the 'daily_leadgen_briefing' utility WhatsApp template in MessagingMe dashboard with a single {{1}} body parameter
6. After filling env vars: `pm2 restart openclaw-leadgen`
7. To test Task F manually (safe, read-only): `cd /home/openclaw/leadgen && node -e "require('./src/tasks/task-f-briefing')('test-manual-run').then(() => console.log('done')).catch(e => console.error(e))"`
8. Check Supabase logs table for the test run entries
  </action>
  <verify>
- PM2 process online with all tasks registered
- All .env vars filled in by user
- Manual Task F test produces log entries in Supabase
  </verify>
  <done>PM2 running with all outreach tasks, external service credentials configured, at least one task tested manually</done>
</task>

</tasks>

<verification>
- PM2 process online with all tasks registered
- All .env vars present (some may be empty pending user configuration)
- No import errors in PM2 logs
- Scheduler shows correct cron expressions for all 7 entries
</verification>

<success_criteria>
- Process starts and stays online in PM2
- All 5 outreach tasks + WhatsApp polling registered at correct cron times
- User has configured required external service credentials
- At least one task (Task F briefing) tested manually with successful log entry
</success_criteria>

<output>
After completion, create `.planning/phases/03-outreach-engine/03-05-SUMMARY.md`
</output>
