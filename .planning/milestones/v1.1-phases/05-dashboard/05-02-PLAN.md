---
phase: 05-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/pages/Home.jsx
  - frontend/src/components/dashboard/FunnelCard.jsx
  - frontend/src/components/dashboard/ActivityCard.jsx
  - frontend/src/components/dashboard/LinkedInGauge.jsx
  - frontend/src/components/dashboard/CronMonitor.jsx
  - frontend/src/components/dashboard/SourceChart.jsx
  - frontend/src/components/dashboard/ScoreChart.jsx
  - frontend/src/components/dashboard/TrendChart.jsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07]

must_haves:
  truths:
    - "User sees conversion funnel in entonnoir shape with percentage between each stage"
    - "User sees leads added today and this week in a separate Activity section"
    - "User sees horizontal progress bar showing LinkedIn invitations x/15"
    - "User sees last run timestamp and status indicator for each cron task A-F"
    - "User sees pie/donut chart of signal source breakdown with tooltips on hover"
    - "User sees bar chart histogram of ICP score distribution with tooltips on hover"
    - "User sees line chart of 7-day pipeline activity trend with tooltips on hover"
    - "Dashboard auto-refreshes data every 2 minutes without manual reload"
  artifacts:
    - path: "frontend/src/pages/Home.jsx"
      provides: "Dashboard layout with all 7 widget components"
      min_lines: 40
    - path: "frontend/src/components/dashboard/FunnelCard.jsx"
      provides: "Recharts FunnelChart with conversion percentages"
      contains: "FunnelChart"
    - path: "frontend/src/components/dashboard/ActivityCard.jsx"
      provides: "Today and this week lead counts"
      min_lines: 15
    - path: "frontend/src/components/dashboard/LinkedInGauge.jsx"
      provides: "Horizontal progress bar with sent/limit display"
      min_lines: 15
    - path: "frontend/src/components/dashboard/CronMonitor.jsx"
      provides: "Grid of cron tasks with status indicators and timestamps"
      min_lines: 20
    - path: "frontend/src/components/dashboard/SourceChart.jsx"
      provides: "Recharts PieChart for signal sources"
      contains: "PieChart"
    - path: "frontend/src/components/dashboard/ScoreChart.jsx"
      provides: "Recharts BarChart for ICP score histogram"
      contains: "BarChart"
    - path: "frontend/src/components/dashboard/TrendChart.jsx"
      provides: "Recharts LineChart for 7-day trend"
      contains: "LineChart"
  key_links:
    - from: "frontend/src/pages/Home.jsx"
      to: "/api/dashboard/stats"
      via: "TanStack useQuery with api.get"
      pattern: "useQuery.*dashboard.*stats"
    - from: "frontend/src/pages/Home.jsx"
      to: "/api/dashboard/charts"
      via: "TanStack useQuery with api.get"
      pattern: "useQuery.*dashboard.*charts"
    - from: "frontend/src/pages/Home.jsx"
      to: "/api/dashboard/cron"
      via: "TanStack useQuery with api.get"
      pattern: "useQuery.*dashboard.*cron"
    - from: "frontend/src/components/dashboard/FunnelCard.jsx"
      to: "recharts"
      via: "import FunnelChart"
      pattern: "import.*FunnelChart.*recharts"
---

<objective>
Build the full dashboard UI replacing the placeholder Home page with 7 interactive widget components consuming the API endpoints from Plan 01.

Purpose: Julien sees his pipeline state at a glance -- funnel, activity, LinkedIn gauge, cron health, and three charts -- all auto-refreshing every 2 minutes.
Output: Complete dashboard page with all DASH-01 through DASH-07 requirements visible and functional.
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-01-SUMMARY.md
@.planning/phases/04-api-auth-react/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard widget components</name>
  <files>
    frontend/src/components/dashboard/FunnelCard.jsx,
    frontend/src/components/dashboard/ActivityCard.jsx,
    frontend/src/components/dashboard/LinkedInGauge.jsx,
    frontend/src/components/dashboard/CronMonitor.jsx,
    frontend/src/components/dashboard/SourceChart.jsx,
    frontend/src/components/dashboard/ScoreChart.jsx,
    frontend/src/components/dashboard/TrendChart.jsx
  </files>
  <action>
SSH to VPS (ubuntu@146.59.233.252). Create directory: `mkdir -p /home/openclaw/leadgen/frontend/src/components/dashboard`

Create 7 React components. All use ESM imports. Style: colorful/vivid marketing dashboard per user decision. White card backgrounds with shadow, rounded corners.

**FunnelCard.jsx** (DASH-01):
- Receives `data` prop: { funnel: {new, invited, connected, email, whatsapp}, conversions: {invited_pct, connected_pct, email_pct, whatsapp_pct} }
- Use Recharts FunnelChart/Funnel/Cell/LabelList/Tooltip components
- Data array: [{name: "New", value: funnel.new}, {name: "Invited", value: funnel.invited}, ...] in descending order
- Vivid gradient colors: indigo-600, violet-500, purple-400, fuchsia-400, pink-300
- Show conversion percentages between stages as labels (e.g., "45%" between New and Invited)
- Display conversion percentages below the chart in a horizontal row of badges
- ResponsiveContainer with height={280}
- Card title: "Funnel de conversion"

**ActivityCard.jsx** (DASH-02):
- Receives `data` prop: { today, week }
- Two big number displays side by side
- "Aujourd'hui" with today count in large bold text (indigo-600 color)
- "Cette semaine" with week count in large bold text (violet-600 color)
- Card title: "Activite recente"
- Small calendar/clock icon or emoji next to each (use unicode, not a library)

**LinkedInGauge.jsx** (DASH-03):
- Receives `data` prop: { sent, limit }
- Horizontal progress bar (per user locked decision)
- Calculate pct = Math.min(100, Math.round((sent/limit)*100))
- Color coding: indigo-500 (normal), yellow-500 (>=80%), red-500 (>=100%)
- Show "sent/limit" as large number and pct as small text
- Use inline style={{ width: `${pct}%` }} for the bar (NOT dynamic Tailwind classes -- they get purged)
- Card title: "Invitations LinkedIn aujourd'hui"

**CronMonitor.jsx** (DASH-04):
- Receives `data` prop: { tasks: [{task, label, status, lastRun}] }
- Grid layout: 2 cols on mobile, 3 on md, 6 on lg (one per task)
- Traffic light indicators: green dot for "ok", red dot for "error", yellow pulsing dot for "running", gray dot for "never"
- Show task label (A - Signaux, etc.) and relative time for lastRun
- For relative time, use a simple helper function: if < 1h ago show "il y a Xmin", if < 24h show "il y a Xh", else show date. Use Intl.DateTimeFormat for date fallback.
- Card title: "Taches cron"

**SourceChart.jsx** (DASH-05):
- Receives `data` prop: array of {name, value}
- Recharts PieChart with Pie, Cell, Tooltip, Legend
- Vivid colors: ["#6366f1", "#f59e0b", "#10b981", "#ef4444", "#8b5cf6", "#ec4899"]
- ResponsiveContainer height={250}
- Tooltip shows exact count on hover (per user locked decision: interactive with tooltips)
- Card title: "Sources de signal"

**ScoreChart.jsx** (DASH-06):
- Receives `data` prop: array of {range, count}
- Recharts BarChart with Bar, XAxis, YAxis, Tooltip, CartesianGrid
- Indigo gradient fill (#6366f1)
- ResponsiveContainer height={250}
- XAxis shows range labels (0-20, 20-40, etc.), YAxis shows count
- Tooltip on hover shows exact count
- Card title: "Distribution scores ICP"

**TrendChart.jsx** (DASH-07):
- Receives `data` prop: array of {date, count}
- Recharts LineChart with Line, XAxis, YAxis, Tooltip, CartesianGrid
- Indigo line (#6366f1) with dot markers
- ResponsiveContainer height={250}
- XAxis shows date (format: day/month, e.g. "21/03")
- Tooltip on hover shows exact count for that day
- Card title: "Tendance 7 jours"
  </action>
  <verify>
Verify all 7 files exist and have correct imports:
```bash
ssh ubuntu@146.59.233.252 "ls -la /home/openclaw/leadgen/frontend/src/components/dashboard/ && head -5 /home/openclaw/leadgen/frontend/src/components/dashboard/*.jsx"
```
  </verify>
  <done>
All 7 dashboard component files created in frontend/src/components/dashboard/ with correct Recharts imports and Tailwind styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Home.jsx with Dashboard page and deploy</name>
  <files>frontend/src/pages/Home.jsx</files>
  <action>
SSH to VPS (ubuntu@146.59.233.252).

**Replace frontend/src/pages/Home.jsx** with the full Dashboard page:

- Import all 7 dashboard components from "../components/dashboard/"
- Import useQuery from "@tanstack/react-query"
- Import api from "../api/client" (existing fetch wrapper with Bearer token)
- Import useAuth from "../context/AuthContext" (for logout button)

**Three TanStack Query hooks** (define inside component or as custom hooks):
1. `useDashboardStats()` -- queryKey: ["dashboard", "stats"], queryFn: () => api.get("/dashboard/stats"), staleTime: 60_000, refetchInterval: 120_000
2. `useDashboardCharts()` -- queryKey: ["dashboard", "charts"], queryFn: () => api.get("/dashboard/charts"), staleTime: 60_000, refetchInterval: 120_000
3. `useDashboardCron()` -- queryKey: ["dashboard", "cron"], queryFn: () => api.get("/dashboard/cron"), staleTime: 30_000, refetchInterval: 60_000 (cron refreshes faster)

**Layout** (per research recommendation):
- Header bar: "Lead Gen MessagingMe" title left, logout button right, white bg with shadow
- Main content: max-w-7xl centered, bg-gray-100 background, space-y-6 vertical sections
- Row 1: FunnelCard (lg:col-span-2) + ActivityCard + LinkedInGauge stacked (lg:col-span-1) in a 3-col grid
- Row 2: CronMonitor full width
- Row 3: 3-col grid with SourceChart, ScoreChart, TrendChart

**Loading states:** Show a skeleton/spinner for each section while data loads. Simple approach: show "Chargement..." text in a card with animate-pulse background.

**Error states:** If a query fails, show error card with "Erreur de chargement" message and a retry button that calls refetch().

**Pass data to components:** Each component receives its data as props. Only render component when data is available (check `isSuccess` or `data` existence).

**Build and deploy:**
```bash
cd /home/openclaw/leadgen/frontend && npm run build
pm2 restart leadgen
```
  </action>
  <verify>
1. Build succeeds without errors:
```bash
ssh ubuntu@146.59.233.252 "cd /home/openclaw/leadgen/frontend && npm run build 2>&1 | tail -5"
```

2. PM2 process stable:
```bash
ssh ubuntu@146.59.233.252 "pm2 status leadgen"
```

3. Dashboard loads in browser -- verify HTML response contains dashboard structure:
```bash
TOKEN=$(ssh ubuntu@146.59.233.252 "curl -s -X POST http://172.17.0.1:3006/api/auth/login -H 'Content-Type: application/json' -d '{\"email\":\"julien@messagingme.fr\",\"password\":\"PLACEHOLDER\"}' | node -e \"process.stdin.on('data',d=>console.log(JSON.parse(d).token))\"")
# The SPA will load and fetch data client-side, so just verify the HTML shell loads
ssh ubuntu@146.59.233.252 "curl -s -o /dev/null -w '%{http_code}' http://172.17.0.1:3006/"
```
Expected: 200

4. Verify no JS errors in build output.
  </verify>
  <done>
Dashboard page loads at https://leadgen.messagingme.app/ after login. Shows conversion funnel with percentages, activity counters (today/week), LinkedIn gauge (x/15 progress bar), cron task status grid with traffic lights, and three charts (source pie, ICP histogram, 7-day trend) with interactive tooltips. Data auto-refreshes every 2 minutes.
  </done>
</task>

</tasks>

<verification>
- Frontend build completes without errors
- PM2 process is online and stable
- Dashboard loads after login at https://leadgen.messagingme.app/
- Funnel displays 5 stages with conversion percentages between them
- Activity section shows today and this week counts separately from funnel
- LinkedIn gauge shows horizontal progress bar with x/15
- Cron monitor shows 6 tasks with colored status dots and timestamps
- Source chart shows pie/donut with tooltips on hover
- Score chart shows bar histogram with tooltips on hover
- Trend chart shows 7-day line with tooltips on hover
- Data auto-refreshes without page reload
</verification>

<success_criteria>
Julien logs into https://leadgen.messagingme.app/ and sees a complete, colorful dashboard with all 7 KPI widgets rendering real data from Supabase. Charts are interactive with hover tooltips. Data refreshes automatically every 2 minutes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard/05-02-SUMMARY.md`
</output>
