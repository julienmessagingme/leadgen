---
phase: 12-browser-signal-collector
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/browser-signal-collector.js
autonomous: true
requirements: [BSIG-03, BSIG-04]

must_haves:
  truths:
    - "Browser searches LinkedIn posts by keyword and extracts post authors as leads"
    - "Browser searches LinkedIn Jobs by keyword and identifies decision-makers at hiring companies"
    - "Keyword post search uses same keywords as Bereach watchlist entries"
    - "Job keyword search finds companies hiring and looks up CX/digital decision-makers"
  artifacts:
    - path: "src/lib/browser-signal-collector.js"
      provides: "Browser-based keyword and job_keyword signal collection"
      exports: ["collectBrowserKeywordSignals", "collectBrowserJobSignals"]
      min_lines: 120
  key_links:
    - from: "src/lib/browser-signal-collector.js"
      to: "src/lib/browser.js"
      via: "navigateWithLimits, humanDelay"
      pattern: "navigateWithLimits"
    - from: "src/lib/browser-signal-collector.js"
      to: "supabase.watchlist"
      via: "query keyword and job_keyword sources"
      pattern: "source_type.*keyword"
---

<objective>
Add keyword post search and job keyword scrapers to the browser signal collector.

Purpose: Replicate Bereach's keyword post search (find post authors by topic) and job keyword search (find decision-makers at hiring companies) using Playwright directly on LinkedIn.
Output: Two new exported functions added to src/lib/browser-signal-collector.js
</objective>

<execution_context>
@C:/Users/julie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-browser-signal-collector/12-CONTEXT.md
@.planning/phases/11-browser-infrastructure/11-01-SUMMARY.md
@.planning/phases/11-browser-infrastructure/11-02-SUMMARY.md
@.planning/phases/12-browser-signal-collector/12-01-SUMMARY.md
@src/lib/browser.js
@src/lib/signal-collector.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add keyword post search scraper to browser-signal-collector.js</name>
  <files>src/lib/browser-signal-collector.js</files>
  <action>
Add collectBrowserKeywordSignals(page, runId) function to browser-signal-collector.js:

1. Load active watchlist entries where source_type = 'keyword' and is_active = true.

2. For each keyword source:
   a. Navigate to LinkedIn search results for posts: https://www.linkedin.com/search/results/content/?keywords={encodeURIComponent(source.keywords)} using navigateWithLimits.
   b. Wait for search results to load.
   c. Extract post authors from the FIRST PAGE of results only (per user decision: Claude's discretion on number of pages, use 1 page to conserve the 100-page budget):
      - For each post result card, extract:
        - Author name (the person who posted)
        - Author headline
        - Author profile URL (link to their profile)
      - Use CSS selectors for LinkedIn search result cards. Each card typically shows author avatar, name (as a link), headline.
   d. humanDelay() between navigations.
   e. Skip posts where author is not extractable (company posts, shared articles without clear author).

3. Format into signal objects:
   - signal_type: "post"
   - signal_category: "sujet" (matching Bereach convention)
   - signal_source: source.source_label || source.keywords
   - source_origin: "browser"

4. Error isolation per source. If rate limit hit, stop and return partial results.

Note: this function receives a `page` parameter (browser already created by the orchestrating function). Does NOT create its own browser context.

Export collectBrowserKeywordSignals.
  </action>
  <verify>
SSH to VPS: node -e "const m = require('./src/lib/browser-signal-collector'); console.log(typeof m.collectBrowserKeywordSignals)"
Should print "function".
  </verify>
  <done>
collectBrowserKeywordSignals navigates LinkedIn post search, extracts post authors (name, headline, URL) from first page, returns formatted signals with source_origin:"browser" and signal_category:"sujet".
  </done>
</task>

<task type="auto">
  <name>Task 2: Add job keyword scraper and test both on VPS</name>
  <files>src/lib/browser-signal-collector.js</files>
  <action>
Part A - Add collectBrowserJobSignals(page, runId) function:

1. Load active watchlist entries where source_type = 'job_keyword' and is_active = true.

2. For each job_keyword source (per CONTEXT: search LinkedIn Jobs AND feed posts):

   a. LinkedIn Jobs search:
      - Navigate to https://www.linkedin.com/jobs/search/?keywords={encodeURIComponent(source.keywords)} using navigateWithLimits
      - Extract company names from the first page of job results (company name is visible on each job card)
      - For each company (take top 3 to conserve page budget):
        - Navigate to the company's LinkedIn page (construct URL or click through)
        - Look for "People" section or key employees section
        - Try to find a CX/digital decision-maker (look for titles containing: directeur, head, VP, chief, responsable + experience client, CX, digital, customer)
        - If found: extract name, headline, profile URL
        - humanDelay() between each navigation

   b. Alternative approach if company page scraping is too complex:
      - Search LinkedIn posts for: "{companyName} directeur experience client OR head of CX OR directeur digital"
      - Extract post authors matching the company
      - This mirrors the Bereach approach in signal-collector.js collectJobSignals

3. Format into signal objects:
   - signal_type: "job"
   - signal_category: "job" (matching Bereach convention)
   - signal_source: (source.source_label || "job") + " | " + jobTitle + " @ " + companyName
   - source_origin: "browser"

4. Error isolation. Rate limit awareness.

Export collectBrowserJobSignals.

Part B - VPS integration test:

5. SSH to VPS, create temporary test script that:
   - Creates browser context
   - Calls collectBrowserKeywordSignals with a test keyword
   - Calls collectBrowserJobSignals with a test job keyword
   - Prints results
   - Closes browser

6. Iterate on CSS selectors if needed. Fix issues.

7. Delete test script after validation.

8. Update any CSS selectors in Task 1's keyword scraper if LinkedIn UI differs from expectations.
  </action>
  <verify>
SSH to VPS: node -e "const m = require('./src/lib/browser-signal-collector'); console.log(typeof m.collectBrowserKeywordSignals, typeof m.collectBrowserJobSignals)"
Should print "function function".

Integration test on VPS shows functions execute without crash (may return 0 results if no matching sources in watchlist, that's fine).
  </verify>
  <done>
collectBrowserJobSignals scrapes LinkedIn Jobs search results, identifies hiring companies, finds decision-makers, returns formatted signals. Both keyword and job_keyword scrapers tested on VPS with real LinkedIn pages.
  </done>
</task>

</tasks>

<verification>
- browser-signal-collector.js exports collectBrowserKeywordSignals and collectBrowserJobSignals
- Both functions use navigateWithLimits (not page.goto)
- Both functions use humanDelay between actions
- Signal objects include source_origin: "browser"
- Error isolation per source
- Page budget conserved (1 page of results per query)
</verification>

<success_criteria>
Browser-based keyword post search and job keyword scrapers work on VPS, extracting post authors and company decision-makers from LinkedIn search results, returning formatted signal objects compatible with the existing pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/12-browser-signal-collector/12-02-SUMMARY.md`
</output>
